# RadioSim Pro 3.0

![RadioSim Pro](../logo.png)

> **対象読者**: **Windows バイナリ版（`RadioSimPro.exe`）を使う方**向けの操作マニュアルです。アプリのヘルプメニュー「ドキュメントを開く」で開くのもこの文書です。
> ソースから動かす・中身に手を入れる場合は [developer_ja.md](developer_ja.md) を参照してください。

地上無線回線の伝搬特性を現地調査前にスクリーニングするデスクトップシミュレーターです。
国土地理院 DEM（数値標高モデル）を自動取得し、地形断面・回折損・植生減衰・リンクバジェットをリアルタイムに可視化します。

---

## 目次

1. [概要・用途](#概要用途)
2. [動作環境](#動作環境)
3. [インストール・起動](#インストール起動)
4. [メニュー](#メニュー)
5. [地図](#地図)
6. [使い方 — 個別シミュレーション](#使い方--個別シミュレーション)
7. [使い方 — 複数経路](#使い方--複数経路)
8. [使い方 — 条件探索](#使い方--条件探索比較--スイープ)
9. [使い方 — 中継経路](#使い方--中継経路)
10. [プロジェクトファイル（.rsproj）](#プロジェクトファイルrsproj)
11. [入力パラメータ詳細](#入力パラメータ詳細)
12. [計算モデル詳細](#計算モデル詳細)
13. [DEM 取得ロジック](#dem-取得ロジック)
14. [保存パッケージ](#保存パッケージ)
15. [アンインストール](#アンインストール)
16. [既知の制限・注意事項](#既知の制限注意事項)
17. [著作権](#著作権)

---

## 概要・用途

RadioSim Pro は、無線回線設計における **現地調査前の概算スクリーニング** に特化したツールです。
TX（送信局）と RX（受信局）の座標・アンテナ高・無線設定を入力すると、国土地理院の標高データを自動取得して地形断面を描画し、リンクバジェットの成立可否を数秒で判定します。

### 主な機能

#### 実行フロー — 「何を調べたいか」で 4 つ

| フロー | 調べたいこと |
| --- | --- |
| **個別シミュレーション** | 1 本の回線が成立するか。地形断面を見ながらアンテナ高・降雨強度をその場で振れる |
| **複数経路** | 多数の回線をまとめて。CSV での入出力に対応 |
| **条件探索（比較 / スイープ）** | **1 本の経路を掘る**。条件を変えた結果を並べて比較（ベース＋最大 5 条件）／1 軸を N 点振って「どこで足りるようになるか」を折れ線＋表で確認。地形の取得は 1 回だけ |
| **中継経路** | 直接届かない回線を中継点で継ぐ。中継点は最大 7 つで、区間ごとにリンクバジェットを計算。**全体の可否は一番苦しい区間**で決まる（再生中継＝中継点で受けて送り直す方式） |

#### 地図

- **地図**（4 モード）: 地図クリックで座標入力／地図から複数経路へ経路を連続追加／中継経路の中継点を地図から追加／DEM キャッシュの可視化・事前取得・削除

#### 計算モデル

- 国土地理院 DEM PNG タイル（5m / 10m メッシュ）から地形断面を自動生成
- 地球曲率補正（標準大気 K = 4/3 固定）
- Bullington 法 / Fresnel-Kirchhoff 法による回折損計算
- 植生減衰（LoS 侵入深さモデル）
- 環境損失（市街地 / 郊外 / 農村 / 見通し の 4 区分）
- 降雨減衰（ITU-R P.838-3）・大気減衰（ITU-R P.676-13 Annex 2）

#### 出力・レポート

- **A4 レポート（v2）**: 経路ごと / サマリを A4 縦の印刷確定枠で出力（**経路ごとは 1 枚に収まり**、サマリ台帳は行数しだいで 2 枚目に入ります）。ブラウザの Ctrl+P でそのまま PDF 化（追加ソフト不要）。案件名・生成日時・ID を載せた自己同定ヘッダ/フッタ付き
- **アンテナ初期指向 AZ/EL**: 相手局へ正対するための真方位・仰角を経路ごとのレポートに両端ぶん表示（初期値。最終調整は現場の RSSI で）
- **経路地図の自動添付**: TX/RX・経路・距離を地図に重ねて HTML レポートへ埋め込み
- **全経路を 1 枚に俯瞰する地図**（サマリレポート・判定で色分け）
- **複数経路レポートの一括印刷**: サマリと全経路を 1 文書へ連結した `report_all.html` を自動生成。Ctrl+P 一発で全ページ分の PDF になります
- 結果のパッケージ保存（PNG / CSV / JSON / HTML / KML）

#### 入力の再利用

- **プロジェクトファイル（`.rsproj`）**: 座標・全パラメータ・案件情報・複数経路の行・条件探索の条件・中継経路を **1 つのファイルに束ねて**保存／再開
- **案件情報（案件名・自由メモ）**: ランチャーで入力すると個別・複数経路どちらのレポートにも反映

#### 画面

- グラフウィンドウでのアンテナ高・降雨強度リアルタイムスライダー
- 座標表記の切り替え（十進度 DD / 度分秒 DMS）
- 日本語 / 英語 UI 切替
- システム連動ダークモード（ライト / ダーク / システム自動）

### 精度の位置付け

DEM の水平解像度は 5〜10m であり、回折損の実質精度は **±5〜15 dB** 程度です。⚠️ **この範囲は、複数の障害物が重なる経路には当てはまりません**——合成損失は実測とも基準実装とも突き合わせていません（後述）。
本ツールはあくまで「現地調査が必要かどうかを判断する」スクリーニング用途を目的としており、回線設計の最終判断には使用しないでください。

---

## 動作環境

| 項目           | 要件                                           |
| -------------- | ---------------------------------------------- |
| OS             | Windows 10 / 11（64 ビット）                   |
| インターネット | DEM 取得時に必要（取得済みタイルはキャッシュ） |

> **Python のインストールは不要です。** 実行に必要なものはすべて同梱されています。

---

## インストール・起動

### インストール

1. 配布 ZIP ファイルを任意のフォルダに展開してください。
2. 展開されたフォルダを任意の場所に置きます。フォルダを移動しても動作します。

> **注意**: フォルダ内の構成は変更しないでください。`RadioSimPro.exe` 単体では動作しません。

### 起動

`RadioSimPro.exe` をダブルクリックします。

初回起動時、exe と同じフォルダに以下のディレクトリ・ファイルが自動作成されます。

| パス                   | 内容                                                               |
| ---------------------- | ------------------------------------------------------------------ |
| `terrain_cache/`     | DEM タイルのディスクキャッシュ（セッションをまたいで保持されます） |
| `results/`           | 保存パッケージの出力先                                             |
| `radiosim_conf.json` | UI 設定・最後に使用した入力値の保存先                              |

---

## メニュー

メニューバーは **ファイル / 設定 / ヘルプ** の 3 つです。以下がその全項目です。

### ファイル

ファイルや OS 側へ出る操作をまとめてあります。

| 項目                     | 説明                                                                                   |
| ------------------------ | -------------------------------------------------------------------------------------- |
| プロジェクトを開く...    | 保存した `.rsproj` を読み込み、入力一式を復元します → [プロジェクトファイル（.rsproj）](#プロジェクトファイルrsproj) |
| プロジェクトを保存...    | 現在の入力一式を `.rsproj` に書き出します → [プロジェクトファイル（.rsproj）](#プロジェクトファイルrsproj) |
| パラメータ読込...        | 過去の設定ファイルから**シミュレーション条件だけ**を入力フォームへ取り込みます         |
| 結果フォルダを開く       | `results/` フォルダをエクスプローラーで開きます                                        |

### 設定

選んだ内容は `radiosim_conf.json` に保存され、次回の起動でも保たれます。

| 項目                 | 選択肢                       | 説明                                                                   |
| -------------------- | ---------------------------- | ------------------------------------------------------------------------ |
| テーマ               | システム / ライト / ダーク   | ウィンドウのカラーテーマ                                                 |
| 言語                 | 日本語 / English（＋自分で足した言語） | UI の表示言語（再起動で反映）→ [表示言語を自分で足す](#表示言語を自分で足す) |
| 座標の表示形式             | 十進度（DD）/ 度分秒（DMS）  | 座標の**表示**表記。→ 下記の注意                                        |
| プロキシ設定...      | URL 入力                     | HTTP プロキシを明示指定（空欄 = OS のプロキシ設定を使用）→ 下記          |
| アプリ設定を読込む... | —                            | 設定ファイルからテーマ・言語・プロキシ**だけ**を取り込みます             |
| 全キャッシュ削除...  | —                            | 取得済みの全 DEM / 地図タイルを削除します（確認あり）                    |

### ヘルプ

| 項目           | 説明                                     |
| -------------- | ------------------------------------------ |
| ドキュメントを開く | このドキュメントをブラウザで表示します     |
| バージョン情報 | インストールされている版数を表示します     |

> **「パラメータ読込」と「アプリ設定を読込む」の違い** — 前者は**シミュレーションの条件**（座標・周波数・アンテナ高など）を、後者は**アプリの見た目と接続設定**（テーマ・言語・プロキシ）を取り込みます。**互いに相手の領分には触りません**ので、他人から受け取ったファイルを読んでも表示言語やネットワーク設定が勝手に変わることはありません。

> **地図はメニューにありません** — ランチャー下部の **「地図」ボタン**から開きます（→ [地図](#地図)）。

### 表示言語を自分で足す

日本語・英語のほかに、**自分で用意した訳を追加できます**。アプリと同じフォルダに `lang` フォルダを作り、`<言語コード>.json` を置いて再起動すると、設定 > 言語のメニューに現れます。

```json
{
  "_name": "Français",
  "btn_run": "Exécuter",
  "menu_help": "Aide"
}
```

- `_name` は**言語メニューに出す名前**です。その言語自身の字で書いてください（自分の言語を探すのは、まだ読めない画面の中だからです）。省略するとファイル名のコードがそのまま出ます。
- **全部を訳す必要はありません。** 書いたキーだけが置き換わり、**残りは英語のまま**出ます。アプリの版が上がって項目が増えても、訳が壊れることはありません。
- キーの一覧は、ソースコードの `core/i18n.py` にあります（<https://github.com/kumahide/radiosim>）。⚠️ **配布版（exe）には含まれません**——このファイルは同梱していないので、上のリポジトリで見てください。
- **`{n}` や `{dir}` のような差し込みは、英語の文と同じものを同じ数だけ残してください。** 違っていると**そのキーは採用されません**（英語のまま表示されます）。採用しなかったものがあると、起動時に**理由ごとに分けて**お知らせします（どの理由で何件・例は 3 つまで）。⚠️ **全部採用できたときは何も出ません**——お知らせが出ないのは「読めていない」ではなく「問題が無かった」という意味です（読めたかどうかは**設定 > 言語**にその言語が並ぶかで分かります）。⚠️ 差し込みを勝手に消した訳をそのまま使うと、その画面を開いた瞬間にアプリが止まります。**採用しないのはそれを防ぐためです。** ⚠️ **差し込みの書き方（`{n:.2f}` の `.2f` の部分）も英語と同じにしてください**——変えられるのは文の中での**位置**だけです。
- **成果物に出る語は対象外**です。レポート（HTML）・KML に加えて、**断面図と条件探索グラフの中の語**（軸の名前・凡例・表題・単位の括り方）、**環境区分の語**（市街地／郊外／田園／見通し）も含みます。成果物の語は読み取る側との約束なので、画面の都合で変えません。⚠️ **これらは画面にも同じ字で出ます**——図は保存すればそのまま成果物になり、環境区分の語はレポートと断面図に焼き込まれるため、**画面の側だけを訳すことはできません**。
- 同梱の `ja` / `en` は上書きできません（`ja.json` を置いても無視されます）。

> ⚠️ **追加した言語では、画面の見切れを保証できません。** ウィンドウの寸法は日本語と英語の語の長さで検査してあり、それより長い訳語では文字が切れることがあります。**非公式な訳**という位置づけでお使いください。

### 座標の表示形式（DD / DMS）

**表示の表記を選ぶ設定**であって、入力モードの切り替えではありません。

- **入力欄はどちらの書き方でも受け付けます。** 設定が DMS でも `34.8, 132.6`（DD）と打てますし、設定が DD でも `34°48'00.0"N, 132°36'00.0"E` と打てます。計算前に必ず十進度へ揃えられます。
- **表記の切り替え・設定やプロジェクトの読み込みを行ったときに、欄の表示が選んだ表記へ整形されます。** 入力を確定しただけでは整形されないので、**打ち込んだ直後は打った形のまま残ります**（異常ではありません）。

### プロキシ設定

企業ネットワーク等でプロキシを経由して DEM タイルを取得する場合は、**設定 > プロキシ設定** からプロキシ URL を入力してください（例: `http://proxy.example.com:8080`）。OK を押すと即時反映されます（アプリの再起動は不要です）。空欄で OK を押すと OS のプロキシ設定に戻ります。

> プロキシが未設定で標高サーバーへ一切とどかない場合、計算は**十数秒で打ち切られ**、プロキシ設定を確認するようダイアログでお知らせします（地形が取れていないまま平坦な地形として完走することはありません）。

---

## 地図

![地図上に送信点・受信点と経路が表示された画面。地図をクリックして座標を拾える](images/shot_map.png)

ランチャー下部の **「地図」ボタン**を押すと、国土地理院の淡色地図を使った補助ウィンドウが開きます。地図はアプリ唯一のインスタンス（ランチャーが所有）で、上部の**モードセレクタ**で 4 つのモード（座標入力／連続追加／中継点／キャッシュ管理）を切り替えます。コアのシミュレーションは地図を開かなくても完結し、地図はあくまで便利機能です。

> 開いたとき、設定中の TX/RX の経路長に合わせて自動でズーム・センタリングします。

### 座標入力モード（既定）

地図をクリックすると**空いている方**（TX → RX の順）を指定し、ランチャーの座標欄（送信/受信）へ自動で書き戻します（数値欄が常に正となります）。両方そろった後は、素のクリックでは何も書きません＝**動かしたい方のマーカーを選んでから**置き直します（下記）。片方を置き直すためにもう片方を潰す必要はありません。

- TX=塗り / RX=白抜きのシアンのマーカーと、両点を結ぶ経路ライン・中点の距離ラベルを表示します。
- ドラッグは地図の移動です（座標は確定したクリックでのみ更新）。
- **ランチャーの座標欄を消す・書き換えると、地図の表示もその場で追従します**（数値欄が正）。欄を空にすれば、素のクリックでまた置き直せます。
- **離れた場所へ置き直すときは地図を右クリック**します（「ここに TX を置く」「ここに RX を置く」）。地図を送ってマーカーが画面の外にあっても、役割を名指しして置き直せます。

### 連続追加モード

複数経路へ経路を地図から次々に積んでいくモードです。モードセレクタの **「複数経路へ連続追加」** を選ぶと複数経路ウィンドウが開き（前面化し）、地図で **TX → RX** のペアを置くたびに複数経路へ 1 行が自動追加され、次の入力へ自動リセットされます（複数経路側で「＋行を追加」を押す必要はありません）。

- 各行の RF（周波数・アンテナ利得・アンテナ高）は、**追加した時点のランチャー値で凍結**されます。あらかじめランチャーで条件を決めてから積んでいく流れです。
- 複数経路にある全経路を地図上に表示します。確定した経路は **座標入力モードと同じ描き方**（TX=塗り / RX=白抜きのマーカー＋経路ライン＋中点の距離ラベル）で示します。ラベルには経路 ID が添えられるので、複数経路の表のどの行かが分かります。⚠️ **TX と RX がほぼ同じ位置にある経路では、2 つのマーカーが重なって見分けられません**（そういう経路は中継経路ウィンドウで扱ってください）。
- 複数経路側の行変更（削除・全クリア・CSV インポート・追加・複製・座標セルの編集確定）は地図の表示へリアルタイムに反映されます。
- 複数経路ウィンドウを閉じると、地図は座標入力モードへ戻ります。

### 中継点モード

中継経路の地点を地図から置いていくモードです。中継経路ウィンドウから地図を開き、モードセレクタの **「中継点を追加」** を選ぶと、クリックのたびに地点が**末尾へ 1 つずつ**足されます（座標入力モードのような TX → RX の交互指定ではありません）。

- 地点列を結ぶ折れ線と、**区間ごとの水平距離バッジ**を表示します。
- ⚠️ **この水平距離が出るのは地図だけです。** 区間表には距離の列が無く、レポートに載るのは**斜距離**（別の量です）。
- 地図は中継経路ウィンドウの写しです。地点の追加・削除はウィンドウ側が正で、地図はそれに追従します。

### 置いた点を動かす

3 つの入力モードとも、地図の上で点を調整できます。**マーカーをクリックして選び**（琥珀色のリングが付き、状態バーに「地点 R1 を選択中」「経路 p1 の RX を選択中」と出ます）、**移したい場所を地図でクリック**します。選択は 1 回の移動で使い切り、**Esc** で取り消せます。

- 規則は 1 つ＝**素のクリックは足す・選んでからのクリックは動かす**。
- 座標入力・連続追加モードでは、**地図の右クリック**でも置き直せます（「ここに TX を置く」「ここに RX を置く」）。マーカーが画面の外にあるときの入口です。
- ドラッグは従来どおり地図の移動です（意図的にそうしています）。ドラッグで点が動く作りにすると、地図をスクロールしようとしただけで入力が黙って書き換わります。
- 正はウィンドウ側です。選んでから動かすまでのあいだに、その点がウィンドウ側で削除・並べ替えされていた場合、地図は移動を断って選び直しを促します（別の点を代わりに動かすことはありません）。
- ⚠️ **地形は 5〜10m メッシュで標本を取ります。** それより小さく動かしたときは状態バーでそう伝えます＝画面上は動いても、同じ地面を拾って結果が変わらないことがあります。

### キャッシュ管理モード

DEM タイルのキャッシュ状況を確認し、任意エリアを事前取得（プリフェッチ）・削除できます。出張先や電波の弱い現場へ行く前に、必要範囲をオフライン用に取得しておく用途です。通常のシミュレーションでも経路周辺のタイルは自動でキャッシュされるため、**この画面を開かなくても支障はありません**。

**カバレッジ表示（自動）** — 地図を移動・拡大縮小すると、キャッシュ済み領域が半透明の色で常時表示されます。色は取得済みの最高精度です。

| 色     | 精度                       |
| ------ | -------------------------- |
| 緑     | 5m メッシュ（航空レーザ測量由来）|
| 黄     | 5m メッシュ（写真測量由来）|
| 水色   | 10m メッシュ               |

色が付いていない範囲は未取得です。

**操作（マウスジェスチャ）**

| 操作                          | 動作                               |
| ----------------------------- | ---------------------------------- |
| ドラッグ                      | 地図を移動                         |
| Ctrl + ドラッグ               | 範囲を選択してダウンロード         |
| Ctrl + Alt + ドラッグ         | 範囲を強制再取得（既存分も再 DL）  |
| Shift + Ctrl + ドラッグ       | 範囲のキャッシュを削除             |

ダウンロード・削除の前には対象エリア数や容量の目安を示す確認ダイアログが表示されます。進捗と結果はウィンドウ下部のステータスバーに表示されます。全キャッシュの削除は **設定 > 全キャッシュ削除** から行えます。

> **外部サーバーへの配慮**: タイルは国土地理院の公共サーバーから取得します。一度取得したタイルは再ダウンロードしません。広域の強制再取得は必要なときだけ行ってください。

---

## 使い方 — 個別シミュレーション

![地形断面グラフ。送受信点を結ぶ見通し線とフレネル第 1 ゾーンが地形に重ねて描かれ、遮蔽区間と受信レベル・マージンが表示されている](images/shot_profile.png)

### 1. ランチャーウィンドウ

起動すると入力フォームが表示されます。

#### サイト情報

| フィールド             | 説明                                          |
| ---------------------- | --------------------------------------------- |
| 送信座標（緯度, 経度） | TX 局の緯度・経度（例:`34.5429, 132.4118`） |
| 受信座標（緯度, 経度） | RX 局の緯度・経度                             |
| 送信アンテナ高（m）    | TX アンテナの地上高                           |
| 受信アンテナ高（m）    | RX アンテナの地上高                           |

#### 無線設定

| フィールド                     | 説明                       |
| ------------------------------ | -------------------------- |
| 周波数（MHz）                  | 周波数（1〜100,000 MHz）   |
| 送信電力（dBm）                | 送信電力                   |
| 送信 / 受信アンテナ利得（dBi） | アンテナ利得               |
| 受信感度（dBm）                | 受信感度（最小受信レベル） |

#### 伝搬環境

| フィールド            | 説明                                       |
| --------------------- | ------------------------------------------ |
| 環境区分              | 市街地 / 郊外 / 農村 / 見通し の 4 区分   |
| 植生高（m）           | 経路全体に一律で当てる**樹冠の平均高さ**。市街地で建物の高さを目安に入れることもできますが、**建物 1 棟ごとの遮蔽はモデル化されません**（「既知の制限」参照）＝あくまで一様なキャノピーとしての近似です |
| ライスKファクター（初期値） | 見通し/散乱電力比の初期値。表示用のみで計算には影響しない（デフォルト = 10.0） |
| 地形の解像度          | 高 約4m 画素 / 中 約8m 画素 / 低 20m 間隔 の 3 段階。⚠️ **段階の名前は「標本を置く画素の大きさ」**であって、*どの標高データを読むか*ではありません（標高は段階によらず**取れる中でいちばん細かい層**から返ります）。画素の実寸は緯度で 3.3〜4.4m と振れるので「約」です。**入れるのは段階だけで、点数はアプリが解きます**。「高」「中」は**経路が通る DEM 画素の縁ごと**に標本を置くので**等間隔ではなく**、点数は経路の長さ・向き・緯度から決まります（「低」だけは 20m の等間隔＝*粗いが速い*段階なので画素を飛ばします）。解決後の点数と**1 画素の寸法**はすぐ下に出ます。既定は「中」 |

#### 案件情報（任意）

| フィールド | 説明                                                         |
| ---------- | ------------------------------------------------------------ |
| 案件名     | レポートのヘッダに載る案件名（個別・複数経路どちらのレポートにも反映） |
| メモ       | 自由メモ。**個別シミュレーションのレポートと、複数経路のサマリー頁**（`summary.html` / `report_all.html`）に載る。**複数経路の経路ごとのレポートには載らない**（サーベイ全体に掛かる情報なので経路ごとに繰り返さない） |

> 案件情報は **ランチャーが入力の源**です。ここで入力した値を、個別シミュレーションの保存レポートも、複数経路も踏襲します。複数経路側では読み取り専用（🔒）で表示され、「↻ ランチャーから更新」で取り込みます。

### 2. 「実行」ボタン（個別シミュレーション）

ボタンを押すと 2 段階でデータを取得します。

1. **DEMタイル事前取得**: TX/RX 間のバウンディングボックス内の全タイルをディスクキャッシュに保存します（最大 8 並列）。キャッシュ済みタイルはスキップされるため 2 回目以降は即完了します。
2. **標高データ取得**: サンプル点ごとに標高を並列取得します（最大 8 並列）。同じ TX/RX 座標・サンプル数であれば前回のキャッシュを即時使用します。

### 3. グラフウィンドウ

取得完了後、地形断面グラフが表示されます。

#### グラフの見方

| 表示要素   | 説明                      |
| ---------- | ------------------------- |
| 茶色の塗り | 地形（地球曲率補正済み）  |
| 緑色の塗り | 植生層（地表高 + 植生高） |
| 赤の破線   | 見通し線（LoS）           |
| シアンの帯 | 第 1 Fresnel ゾーン       |
| 黒の縦棒   | TX / RX アンテナ          |

⚠️ **地形の輪郭が細かくギザギザ／階段状に見えるのは正常です**——断面図は**標高を読んだ点をそのまま結んだ線**で、「高」「中」では**数 m ごと**に読むためです（「高」は約 4m、「中」は約 8m の格子の境目ごと）。**標高データが荒いのでも壊れているのでもなく**、細かく見るほど地形の凹凸がそのまま出ます（「高」では 1 画素の中の標高が一定なので、とくに「棚と縁」の形がはっきり出ます）。同じことは保存される `terrain_profile.csv` の行にも出ます（下の「保存パッケージ」の節を参照）。「低」は 20m の等間隔なので線はなめらかですが、その代わり細かい起伏を飛ばしています。

#### スライダー

| スライダー     | 範囲        | 説明                            |
| -------------- | ----------- | ------------------------------- |
| 送信アンテナ高 | 0〜150 m    | TX アンテナ高をリアルタイム変更 |
| 受信アンテナ高 | 0〜150 m    | RX アンテナ高をリアルタイム変更 |
| 降雨量         | 0〜100 mm/h | 降雨強度をリアルタイム変更      |

スライダーを動かすと 50ms のデバウンス後に自動再計算されます。

#### 保存ボタン

現在の表示状態を `results/YYYYMMDD_HHMMSS/` に保存します（→ [保存パッケージ](#保存パッケージ)）。

### 4. 入力値の自動保存

個別シミュレーションを実行すると、そのときの入力値が `radiosim_conf.json` へ自動保存され、次の起動時に復元されます。**明示的に保存する操作は要りません。**

過去の条件をファイルから取り込みたい場合や、入力一式をまとめて持ち運びたい場合は、[ファイルメニュー](#ファイル)（**パラメータ読込** / **プロジェクトを保存・開く**）を使います。

---

## 使い方 — 複数経路

![複数経路の入力表。1 行 1 経路で、実行後の判定（OK / NG）と水平距離が各行に返っている](images/shot_batch.png)

ランチャーの **複数経路** ボタンで専用ウィンドウを開きます。

### 設計思想 — 個別で詰め、複数経路で確定する

**個別（ランチャー）＝条件を詰める場／複数経路＝確定した条件で成果物を作る場**です。ランチャーが唯一の「真実の源」で、複数経路の各行は **行を追加した時点のランチャー欄をコピーして凍結した確定リンク** になります。

### 入力方法

**手動入力**: テーブルに直接 ID・座標・アンテナ高・周波数・送受信利得を入力します。行の追加・削除・ドラッグ並べ替え・セルの直接編集ができます。

表の右端の **水平距離(m)** 列は TX/RX 座標から自動計算される読み取り専用の表示です（座標を確定するたびに更新）。座標の打ち間違いは距離が極端な値になって現れるため、実行前に気づく手がかりになります。

- **＋行を追加**: その時点のランチャー欄（座標・周波数・利得・アンテナ高）をコピーして凍結した行を追加します。
- **地図から選択**: 地図を「複数経路へ連続追加」モードで開きます。地図で **TX → RX** のペアを置くたびに 1 行が自動で足されます（このウィンドウから地図を開けるようになったのは 2.7 からで、それまでは地図の側からしか始められませんでした）。
- **行を右クリック**: 行ごとのメニューが開きます。
  - **→ 個別へ送る**: その行の座標＋RF をランチャーへ読み込み、調整できます。
  - **⟳ RF をランチャーで更新**: ランチャーの現在の RF をその行へ書き戻します（**座標は保持**）。
  - 複製 / 削除。

**CSV インポート**: テンプレートボタンでサンプル CSV を保存し、編集後にインポートします。

#### CSV フォーマット

必須列: `id, start, end, h_tx, h_rx`

省略可能列: `freq, gain_tx, gain_rx, note`

```csv
id,start,end,h_tx,h_rx,freq,gain_tx,gain_rx,note
path01,"34.54, 132.41","34.53, 132.40",30.0,10.0,2400,12.5,8.0,メイン回線
path02,"34.55, 132.42","34.52, 132.39",20.0,15.0,,,,サブ回線
```

- `start` / `end` は「緯度, 経度」の形式でダブルクォートで囲んでください
- `freq` / `gain_tx` / `gain_rx` を省略した場合は共通設定の値が使われます（**経路ごとに変えられるリンク識別属性**です）。環境区分・降雨強度・回折モデルは共通設定で一括指定します
- `gain_tx` / `gain_rx` 列のない旧 CSV もそのまま読めます（後方互換・利得は共通設定を継承）
- 列名は**大小文字と前後の空白を区別しません**（`ID,Start,…` でも読めます）。`id` は**大小文字を区別せず**一意である必要があります（`p01` と `P01` は同じ出力フォルダになるため重複として弾かれます）

### 共通設定（ランチャーのスナップショット）

ウィンドウ上部の **共通設定** は、経路ごとの個別設定が指定されていない場合に使われるデフォルト値です。**読み取り専用**で、ランチャー（真実の源）のスナップショットとして表示されます。**↻ ランチャーから更新** ボタンでランチャーの現在値を取り込めます。

### 実行と結果

**実行** ボタンを押すと経路を順次処理します。処理中は進捗（処理済み / 全体）が帯に出て、**判定は終わった経路の行へ返ります**。

完了後、`results/batch_YYYYMMDD_HHMMSS/` に以下が保存されます。

| ファイル                     | 内容                                               |
| ---------------------------- | -------------------------------------------------- |
| `summary.html`             | 全経路のサマリーレポート（グラフのサムネイル付き） |
| `summary.csv`              | 全経路の数値結果（スプレッドシートで開けます・距離は m）|
| `summary.kml`              | OK / NG / エラーを色分けした Google Earth 用 KML   |
| `report_all.html`          | サマリ＋全経路を 1 文書へ連結（Ctrl+P 一発で全ページ分の PDF）|
| `{id}/report.html`         | 経路ごとの詳細レポート（地形断面グラフ＋経路地図を埋め込み）|
| `{id}/profile.png`         | 地形断面グラフ                                     |
| `{id}/path.kml`            | 地形・LoS・Fresnel ゾーン・遮蔽区間の 3D KML       |
| `{id}/settings.json`       | 経路ごとの入力パラメータ                           |
| `{id}/terrain_profile.csv` | 地形断面データ                                     |
| `{id}/report.txt`          | テキスト形式のリンクバジェットレポート             |

---

## 使い方 — 条件探索（比較 / スイープ）

![条件探索の比較画面。1 本の経路に対し周波数と利得を変えた 4 条件の受信レベルとマージンが並び、最後の条件だけ NG になっている](images/shot_scenario.png)

**1 本の確定した経路を、条件を変えて掘る**ための画面です。ランチャーの「条件探索」ボタンで開きます。経路（座標）と地形の解像度はランチャーの値に固定され、**地形データの取得は 1 回だけ**で、そのうえで条件を N 通り計算します（同じ経路で何度も回しても DEM の再取得は起きません）。

上部のトグルで 2 つのモードを切り替えます。ランチャー側で座標やパラメータを変えたときは、右上の **「↻ ランチャーから更新」** で取り込み直してください（**押すまで画面の値のまま計算します**＝画面と計算がずれません）。

### 比較（ベース＋条件 N）

- 左端の **ベース**列はランチャーの現在値（読み取り専用）＝**基準は動かしません**。
- **条件 1 … 条件 5** の列に、変えたい欄だけ書き換えます（初期値はベースと同じ）。「＋ 条件を追加」で最大 5 列まで増やせます。
- 変えられるのは周波数・送信電力・送受信アンテナ利得・受信感度・送受信アンテナ高・植生高・降雨強度・環境・回折モデルです。**座標と地形の解像度は変えられません**（経路そのものの比較は複数経路の仕事）。
- レポートは項目を縦に並べた差分表で、**ベースとの差をセル内に併記**します（例 `-76.44 (+13.56)`）。差が出た行は地の色が変わります。

### スイープ（1 軸 N 点）

- **軸**（アンテナ高・周波数・降雨強度など）と範囲・**点数**（2〜41 点）を指定します。
- 範囲の指定は 2 通りから選べます。**開始 / 終了**（絶対値で指定）と、**ベース ±**（今の値を中心に前後へ振る）。**ベース ±** は「今の 30m から ±10m で見たい」という考え方をそのまま入れられます。切り替えても振り幅は引き継ぎ、内部では常に開始/終了へ直してから計算します（プロジェクトファイルにも開始/終了で保存されます）。
- レポートは**折れ線＋数値表**です。折れ線には判定のしきい値（マージン 0 dB）が引かれ、点は判定で塗り分けられます（OK＝緑 / NG＝橙）。色の変わり目が「アンテナ高を何 m にすれば足りるか」です。点数を最大（41 点）にしても **A4 縦 1 枚**に収まります（表は 1 列のまま、行を詰めて収めます＝上から下へ変化を追えます）。
- マージンの振れ幅が大きいとき（深く塞がれた経路を含むとき）は縦軸を対数寄りに切り替えて、しきい値付近が潰れないようにしています。

### 実行と結果

**実行**を押すと、地形取得 → 条件計算 → レポート生成の順に進み、進捗バーが最後まで伸びます。完了すると保存先を告げるダイアログが出て、そのままレポートを開けます（個別・複数経路と同じ流儀）。あとから見るときはランチャーの「結果フォルダ」から開いてください。

`results/scenario_YYYYMMDD_HHMMSS/` に以下が保存されます。

| ファイル          | 内容                                                     |
| ----------------- | -------------------------------------------------------- |
| `scenario.html` | A4 縦 1 枚のレポート（比較＝差分表 / スイープ＝折れ線＋表）|
| `scenario.csv`  | 全条件・全点の数値（スプレッドシートで開けます）           |

---

## 使い方 — 中継経路

![中継経路の画面。送信点・中継点・受信点の 3 地点と、区間ごとの受信レベル・マージン・判定が表に返っている](images/shot_multihop.png)

**1 本の回線を中継点で継ぐ**ときの画面です。ランチャーの **中継経路** ボタンで開きます。山を挟んで直接は届かない 2 点を、見通しの利く尾根などに中継点を置いて繋ぐ検討に使います。

### 前提 — 再生中継（受けて送り直す）

中継点で**いったん受信し、あらためて送信する**方式（再生中継）を前提にしています。したがって:

- 区間ごとに独立したリンクバジェットを計算します（**区間の損失は足し合わせません**）。
- **全体の可否は、一番余裕の少ない区間**で決まります。レポートには全体判定と**区間ごとの内訳の両方**を載せます（どこが苦しいかが分からないと、中継点を足すのかアンテナを上げるのかを決められないため）。
- **反射板（パッシブリフレクタ）による中継は対象外**です。損失の連なり方が別物で、このツールの計算には入っていません。

### 地点と区間

- **地点の表**が入力面です。**先頭が送信点・末尾が受信点**で固定され、「地点を追加」で足した点は**その手前に中継点として挿さります**（並びがそのまま経路の順序です）。役割（送信 / 中継 / 受信）は行に表示されます。
- **行の `＋` を押すと、その行の下に中継点が挿さります**——間に 1 点足したいときに、後ろの地点を消して打ち直す必要はありません。受信点の行に `＋` が出ないのは、末尾が受信点で固定だからです。⚠️ **地点の名前は振り直しません**（`R1` を「東の丘」に直した入力を上書きしないため）。並びは行の順序と役割の表示が示します。
- 地点ごとに **座標**と**アンテナ高**を入力します。**中継点の行には `×` が付き、任意の地点だけを消せます**（送信点・受信点は消せないので `×` は出ません）。中継点を消すと、その地点から出ていく区間の設定も一緒に消えます。**挿したときは逆に、その地点から出ていく区間が空欄で増えます**（同じ位置で挿して消せば元どおりです）。
- 座標は**十進度でも度分秒でも入力できます**。入力を確定すると、設定（設定 > 座標の表示形式）の表記へ整形されます。**高さは地点にだけあります**——中継点のアンテナは 1 本なので、「前の区間の受信高」と「次の区間の送信高」に別々の値を入れられないようにしてあります。
- **区間の表**では、区間ごとに **周波数・送信利得・受信利得**を上書きできます（空欄なら共通設定＝ランチャーの値を使います）。中継点で送受のアンテナが別物になるケースを表現するためです。
- ウィンドウ上部の **共通設定** には、実行に効く前提が **11 項目**（周波数・送信出力・送信利得・受信利得・受信感度／植生高・K ファクター・地形の解像度・降雨強度・環境区分・回折モデル）並びます。複数経路ウィンドウと同じ項目・同じ見せ方です。**読み取り専用**で、ランチャー（真実の源）のスナップショットとして表示され、**↻ ランチャーから更新** で現在値を取り込めます。⚠️ 周波数・送信利得・受信利得は上のとおり区間ごとに上書きできるので、ここに出るのは**空欄のときに引き継がれる値**です。
- **地図から選択**を押すと地図が中継点モードになり、クリックした順に地点へ座標が入ります。地図には地点を順に結ぶ折れ線が引かれ、**区間ごとの水平距離**が線の中点に表示されます（区間の表には距離の列が無いので、区間がどれくらいの長さかはここで読めます）。
- 送信点・受信点の座標は、**ウィンドウを開いた時点のランチャーの値**が初期値として入ります（中継点は空のまま）。
- 中継点は最大 7 つ（区間は最大 8 つ）です。

> **⚠️ 中継点は「置く」もので「動かして探る」ものではありません。** 1 区間ごとに標高データを取りに行くため、動かすたびに新しい取得が走ります。高さや条件を振って探るのは[条件探索](#使い方--条件探索比較--スイープ)の役目です。

### 実行と結果

**実行**を押すと区間を順に処理し、区間ごとの結果（受信レベル・マージン・判定）が一覧に積み上がります。完了すると全体判定（一番苦しい区間の値）が表示されます。

⚠️ **全体判定は区間と同じ 3 値（OK / NG / ERR）です。** 判定できなかった区間（ERR）が 1 つでもあると、全体判定も **ERR** になります——「計算できたが回線が成立しない（NG）」とは別物で、この場合は全体のマージンも数字ではなく `—` になります（判定できなかった区間があるので、経路ぜんたいの余裕は語れません）。どの区間が ERR かは区間の表で読めます。

`results/multihop_YYYYMMDD_HHMMSS/` に以下が保存されます。

| ファイル              | 内容                                    |
| ----------------- | ------------------------------------- |
| `route.html`      | 経路全体の合成シート（全体判定＋区間別の内訳＋俯瞰地図）          |
| `report_all.html` | 合成シート＋全区間の詳細を 1 文書へ連結（Ctrl+P で一括 PDF） |
| `hops.csv`        | 区間別の数値（1 行 = 1 区間・スプレッドシートで開けます）      |
| `{id}/`           | 区間ごとのパッケージ（個別シミュレーションと同じ構成）           |

---

## プロジェクトファイル（.rsproj）

**入力一式を 1 つのファイルに束ねて**保存し、あとで続きから再開するための仕組みです。メニューの **ファイル → プロジェクトを保存 / プロジェクトを開く** から使います。

### 束ねるもの・束ねないもの

| 含む                        | 含まない                          |
| ------------------------- | ----------------------------- |
| 座標・全パラメータ（ランチャーの入力一式）     | **計算結果**（結果は `results/` 側の仕事） |
| 案件情報（案件名・自由メモ）            | **アプリ設定**（テーマ・言語・プロキシ）        |
| 複数経路の行              | ウィンドウの位置・大きさ・開閉状態             |
| 条件探索の条件（比較の列 / スイープの軸と範囲） |                               |
| 中継経路の地点と区間                |                               |

**テーマ・言語・プロキシを含めないのは意図的です**——他人から受け取ったプロジェクトを開いた瞬間に、表示言語やネットワーク設定が変わってしまわないようにしています。

### 保存するとき

- 開いているウィンドウ（複数経路 / 条件探索 / 中継経路）からは**そのときの値**が保存されます。
- **閉じたウィンドウの内容も保存されます**（前回の値を持ち越します）。複数経路のウィンドウを閉じてから保存しても、行は消えません。
- 数値として読めない値が入っている部分は保存せず、その旨をダイアログでお知らせします（壊れたファイルを黙って作らないため）。

### 開くとき

- **開いているウィンドウは閉じません**。上に「プロジェクトを読み込みました。このウィンドウの内容を差し替えられます。」の帯が出るので、**「内容を取り込む」を押したときだけ**そのウィンドウの内容が入れ替わります（押さなければ画面は変わりません）。帯は `×` で閉じられます。
- ランチャーの入力欄と案件情報はその場で入れ替わります。
- **閉じているウィンドウの内容は、そのウィンドウを開いたときに反映されます**（このアプリは「ウィンドウを開いた時点の値で固定する」流儀なので、それに合わせています）。
- 取り込まないままウィンドウを閉じた場合は、**画面に見えていた内容のほうが残ります**（次に保存すると、そちらが書き出されます）。
- 新しい版の RadioSim で保存されたファイルは開けません（黙って壊さないため、その旨を表示します）。

---

## 入力パラメータ詳細

### バリデーション範囲

| パラメータ   | 最小                           | 最大    | 単位 |
| ------------ | ------------------------------ | ------- | ---- |
| 周波数       | 1                              | 100,000 | MHz  |
| 送信電力     | -30                            | 60      | dBm  |
| 送受信利得   | 0                              | 60      | dBi  |
| 受信感度     | -130                           | -20     | dBm  |
| アンテナ高   | 0                              | 500     | m    |
| 植生高       | 0                              | 100     | m    |
| ライスK      | 0                              | 30      | —   |
| 地形の解像度 | 高 / 中 / 低                  | —      | —   |
| 降雨強度     | 0                              | 200     | mm/h |
| 環境区分     | 市街地 / 郊外 / 農村 / 見通し | —      |      |
| 回折モデル   | bullington / single            | —      |      |

---

## 計算モデル詳細

### 地球曲率補正

実際の電波伝搬では大気の屈折により電波は地球の曲率より大きく曲がります。これを等価地球半径係数 K でモデル化します。本ツールでは標準大気（K = 4/3 ≈ 1.333）を内部固定値として使用します。

```
等価地球半径 Re = R_earth × K  （K = 4/3 固定）
曲率補正量 Δh(d) = d × (D - d) / (2 × Re)  [m]
```

| K の値       | 意味                                 |
| ------------ | ------------------------------------ |
| 4/3 ≈ 1.333 | 標準大気（本ツール使用値・固定）     |
| K > 4/3      | 大気ダクト（電波がより大きく曲がる） |
| K < 4/3      | 負の屈折（電波が地面側に曲がる）     |

### Fresnel ゾーン

電波は幾何光学的な直線だけでなく、その周囲の楕円体状の領域（Fresnel ゾーン）を伝搬します。第 1 Fresnel ゾーンが地形・植生に遮蔽されると回折損が発生します。

```
r₁(d) = √(λ × d₁ × d₂ / (d₁ + d₂))
```

### 回折損

#### Bullington 法（デフォルト・ITU-R P.526 §4.5.1）

複数の障害物をまとめて 1 枚の**等価ナイフエッジ**に置き換えるモデルです。両端から地形を見た仰角が最大の点へ引いた 2 本の接線の交点を障害物の位置とし、標準の補正項 `(1 - exp(-Luc/6)) × (10 + 0.02 × d[km])` を掛けます。

🔑 **3.0 で従来の独自 Deygout 実装から差し替えました**（履歴は「既知の制限」）。理由は精度ではなく**連続性**です＝従来の実装は「障害物を数える」構成だったため、**植生高やアンテナ高を 1m 動かすだけで障害物の枚数が変わり、回折損が 84 dB 跳ぶ**ことがありました（アンテナを 1m 上げたら 84 dB 悪くなる）。等価ナイフエッジの位置は地形が動けば連続に動くので、この跳びが構造的に起きません。

⚠️ **「ITU-R P.526 準拠」とも「P.452 準拠」とも名乗りません**。採ったのは P.526 §4.5.1 の**この 1 手法だけ**で、P.452 §4.2.1 の完全法（Delta-Bullington）が足す**球面回折の項**も、clutter も ducting も持ちません。⇒ **標準の手法と同じ値にはなりません**（上の「既知の制限」を参照）。

#### Fresnel-Kirchhoff 損失 J(ν)

```
J(ν) = 6.9 + 20 × log₁₀(√((ν - 0.1)² + 1) + ν - 0.1)  [dB]  （ν > -0.8）
J(ν) = 0                                                          （ν ≤ -0.8）
```

#### Single 法

全サンプル点の ν を計算し、最大値のみで J(ν) を算出するシンプルなモデルです。

### 植生減衰

LoS 線が植生頂点に侵入した深さから経験的に推定します。

```
侵入深さ(d)   = clip(veg_top(d) - LoS(d), 0, 植生高)
重み(d)       = clip(侵入深さ / r₁(d), 0, 1)
有効植生長    = Σ[重み(d)] × サンプル間隔
Veg Loss      = min(有効植生長 × coeff, 45 dB)
```

> ⚠️ **侵入深さの上限は植生高です**＝電波が植生の中を通る厚みは高々キャノピー高であり、
> それを超える分は地表そのものが見通しを切った量＝回折損の担当です。
> **植生高 0m なら植生減衰は必ず 0 dB**になります（3.0 で是正）。

| 周波数帯   | coeff         |
| ---------- | ------------- |
| 1 GHz 未満 | 0.12 × f^0.5 |
| 1〜6 GHz   | 0.20 × f^0.7 |
| 6 GHz 超   | 0.35 × f^0.9 |

### 環境損失

地形・植生損失では捉えきれないマルチパス・散乱等を経験的に補正します。

```
Env Loss = base + slant_dist × dist_c
```

> ⚠️ **遮蔽に由来する量（F1 遮蔽率・回折損）は含みません**。地形が見通しを切ったことは
> 回折損がすでに表しており、環境損失に再び換算すると二重計上になるためです（3.0 で除外）。

| 環境区分 | base | dist_c | min | max  |
| -------- | ---- | ------ | --- | ---- |
| 市街地   | 10.0 | 1.20   | 6.0 | 30.0 |
| 郊外     | 6.0  | 0.80   | 3.0 | 30.0 |
| 農村     | 4.0  | 0.50   | 2.0 | 25.0 |
| 見通し   | 2.0  | 0.30   | 1.0 | 15.0 |

### 降雨減衰（ITU-R P.838-3）

```
γ_R = k × R^α  [dB/km]
Rain Loss = γ_R × d_slant
```

2.4 GHz では降雨の影響は非常に小さく（100 mm/h で約 0.1 dB/km 程度）、実用的な影響が出るのはおおむね 10 GHz 以上からです。

### 大気減衰（ITU-R P.676-13 Annex 2 簡易式）

```
γ_total = γ_O₂ + γ_H₂O  [dB/km]
Gas Loss = γ_total × d_slant
```

### リンクバジェット

```
EIRP      = P_tx + G_tx                             [dBm]
FSPL      = 20×log₁₀(d) + 20×log₁₀(f) - 147.55   [dB]
Total Loss = FSPL + Diff + Veg + Env + Rain + Gas   [dB]
P_rx      = EIRP + G_rx - Total Loss                [dBm]
Act Margin = P_rx - Sensitivity                     [dB]
Status    = OK（≥ 0 dB）/ NG（< 0 dB）
```

---

## DEM 取得ロジック

### 使用データソース

| レイヤー ID   | 解像度               | ズーム | カバレッジ     |
| ------------- | -------------------- | ------ | -------------- |
| `dem5a_png` | 5m（航空レーザ測量） | 15     | 都市部・山地   |
| `dem5b_png` | 5m（写真測量）       | 15     | dem5a より広域 |
| `dem_png`   | 10m（基盤地図情報）  | 14     | 全国           |

`dem5a_png` → `dem5b_png` → `dem_png` の順に試みます。上位レイヤーで 404 またはデータ欠損の場合は次のレイヤーに降格します。

### キャッシュ戦略

- **タイル事前取得**: シミュレーション実行時に TX/RX のバウンディングボックス内の全タイルを事前にディスクキャッシュへ保存（オフライン利用・複数経路の実行の高速化に寄与）
- **ディスクキャッシュ**: `terrain_cache/` に保存、アプリを終了しても保持されます
- **地形キャッシュ**: 同じ TX/RX 座標・サンプル数なら DEM 再取得をスキップ（アプリ再起動でリセット）

---

## 保存パッケージ

### 個別シミュレーション

保存ボタンで `results/YYYYMMDD_HHMMSS/` に以下を保存します。

| ファイル                | 内容                                   |
| ----------------------- | -------------------------------------- |
| `profile.png`         | 地形断面グラフ（150 dpi）              |
| `report.html`         | 詳細レポート（A4 縦 1 枚・地形断面グラフ＋経路地図＋アンテナ初期指向 AZ/EL を埋め込んだ HTML）|
| `path.kml`            | Google Earth 用 3D KML                 |
| `settings.json`       | 入力パラメータ一式（再読み込み可）     |
| `terrain_profile.csv` | 地形断面データ                         |
| `report.txt`          | テキスト形式のリンクバジェットレポート |

> **A4 レポート（v2）**: `report.html` / `summary.html` は **A4 縦の印刷確定枠**で生成されます（**`report.html` は必ず 1 枚**に収まり、`summary.html` の台帳は **27 行を超えると 2 枚目**に入ります＝下の「結果の取扱に関する補足」の項も参照）。ブラウザで開いて **Ctrl+P →「PDF に保存」** でそのまま A4 の PDF になります（追加ソフト不要）。印刷ダイアログでは **「ヘッダーとフッターをオフ」** にしてください（レポート自身が案件名・生成日時・ID を載せた自己同定ヘッダ/フッタを持ちます）。案件名・自由メモはランチャーの「案件情報」欄で入力した値が反映されます。
>
> **一括印刷（`report_all.html`）**: 実行完了時のダイアログで **「全ページを開く」** を選ぶとこのファイルが開きます（「サマリを開く」は従来どおり `summary.html`）。複数経路では、サマリと全経路のレポートを 1 文書へ連結した `report_all.html` も保存されます。**これを開いて Ctrl+P すれば全ページ分の PDF が一度に**できます（1 ページ目＝サマリ台帳、2 ページ目以降＝経路ごとの A4 1 枚）。台帳のサムネイルをクリックすると同じ文書内の該当経路へ飛びます。従来どおり `summary.html` と `{id}/report.html` も個別に残るので、1 経路だけ共有したいときはそちらを使ってください。経路数が多いと連結ファイルは大きくなります（断面図を埋め込むため）。
>
> **経路地図の添付**: `report.html`（個別）には TX/RX・経路・距離を国土地理院 淡色地図へ重ねた静的地図が、`summary.html`（複数経路）には **全経路を 1 枚に俯瞰する地図**（北が上・判定で色分け）が埋め込まれます。地図タイルを取得できない環境では地図を省き、その旨の注記を残してレポート自体は必ず生成します。
>
> **アンテナ初期指向 AZ/EL**: `report.html` の Site Info に、相手局へ正対するための真方位 AZ・仰角 EL を両端ぶん表示します（既存データからの幾何計算＝初期値。最終調整は現場の RSSI 最大化で）。AZ は **真方位** のため、磁気コンパスで向ける場合は地域の偏角（日本では磁北が真北より西へ約 7〜9°・地域差あり）を補正してください。

### 複数経路

`results/batch_YYYYMMDD_HHMMSS/` に保存します。

| ファイル         | 内容                                 |
| ---------------- | ------------------------------------ |
| `summary.html` | 全経路サマリー（サムネイル付き）     |
| `summary.csv`  | 全経路の数値結果                     |
| `summary.kml`  | 全経路の Google Earth 用 KML         |
| `report_all.html` | サマリ＋全経路の連結レポート（一括印刷用）|
| `{id}/`        | 経路ごとのパッケージ（個別と同構成） |


### 帳票の「結果の取扱に関する補足」節

**レポート HTML 4 種（個別・複数経路の台帳・中継経路・条件探索）と `report.txt` の下部に、この節が入ります**（3.0 から）。成果物は印刷して人に渡した先で一人歩きし、**受け取った人は本書も画面の注記も読みません**。そこで**前提**と**その計算に使った式の適用範囲**を、帳票そのものに載せています。

- **常に載るもの**: 標高データは**地表面モデル**で建物・樹木の高さを含まない／植生高は**入力した一律の値**を経路全体に当てている／環境損失は**区分から引いた経験値**／**地面反射（2 波干渉）は考慮していない**／**地球曲率は標準大気の K = 1.33 固定で、大気の屈折がそこから外れる時間帯（サブリフラクション・ダクト）は見ていない**／**画面と帳票に出る「現在のライスKファクター」は回折損からの経験的な推定**で、計算には使っておらず実測とも突き合わせていない／**この結果を出したときの地形の解像度の段階**（段階を細かくすると回折損が増えます＝「低」の 20m 間隔と「高」の画素の縁ごとで最大 +407%）。
- **条件によって載るもの**: 回折モデルが `Bullington` のとき「離れた 2 つ以上の尾根がある経路では小さめに出る／球面回折の項は含めていない」／**降雨・大気の減衰が範囲外につき 0 dB として扱われた**とき（1 GHz 未満）その旨／**植生減衰の係数の定義域（1〜6 GHz）の外側**で計算したとき／**植生高を入れた回線**では「地形が見通しを切る区間で、回折損と植生減衰を重ねて計上している（合計は安全側に大きめ）」旨。⚠️ **降雨率 0 の回線に降雨の注記は出ません**（計算していない項目の限界は書きません）。**植生高 0 の回線に植生の注記も出ません**。
- **1 枚に複数の回線を載せる帳票**（複数経路・中継経路・条件探索）では、**どれか 1 本にでも当てはまる注記**が出ます。
- **較正プロファイル**の欄が付きます。いまは常に **未適用**＝**実測との突き合わせは行っていません**。

⚠️ **この節が増えたことで数字は 1 つも変わりません**（書いていなかったことを書くようにしただけです）。⚠️ 複数経路の台帳は **A4 1 枚に 27 行**までになります（節のぶん減った 4 行のうち、**1 行は 3.0 で節の字を要点だけの箇条書きに詰めたぶん戻りました**。超えると 2 枚目に入ります）。

### 出力列の仕様（出力契約）と変更規約

集計スクリプトや表計算の数式は、**列の名前と並びをそのまま参照します**。そこでこのアプリは、成果物 CSV の**ファイル名・列名・列の並び・値の書式を「契約」として扱い**、次の規約で運用します（3.0 で明文化）。

- **列の追加は末尾のみ。** 既存の列の位置は動かしません（位置で読む相手が居るため）。
- **列の削除・改名・意味の変更は、1 つ前の版で予告してから。** 予告は CHANGELOG と本書の両方に載せます（片方だけだと、配布版だけを見る方に届きません）。
- **値の書式（単位・桁・頭打ち）も契約です。** 単位を変えるときは列名も変えます（例＝2.5 の `slant_km` → `slant_m`）。名前が変われば、古い数式は黙って通らず落ちます。
- **ファイル名も契約です。** 「1 行が何を表すか」が変わるときは、列を足さずに**別のファイル**にします（例＝中継経路の `hops.csv` は、`summary.csv` に区間の列を足す代わりに切り出したものです）。

⚠️ **この規約は「二度と変えない」約束ではなく、変えるときに通る道です。**

> 🔴 **3.0 での書式変更（規約 3 の適用）**: **dB 系の値の桁を 0.01 → 0.1 に変えました**（`-93.20` → `-93.2`）。対象は `rx_dbm` `margin_db` `fspl_db` `diff_db` `veg_db` `env_db` `rain_db` `gas_db` `total_loss_db` と 2 つの利得の列で、**`summary.csv` / `hops.csv` / `scenario.csv` の 3 本ともです**。**列名もファイル名も変わりません**——`-93.2` は `-93.20` と同じ数なので、**数値として読んでいる数式はそのまま動きます**。⚠️ **文字列として桁数を当てにしている処理があれば直してください。** 理由は「0.01 dB がこの計算の持っていない精度だから」です（標高データは水平 5〜10m の網で標高自体にも数 m の誤差、植生高は一律の仮定値、環境損失は区分の経験値）。**計算そのものは従来どおりの精度で行い、見せ方だけを落としています。**

⚠️ **自由文字列の列（`note` / `error` / `label`）は、`=` `+` `@` などで始まる値に `'` を前置します**（表計算ソフトが数式として解釈するのを防ぐため）。数値として読める値（マージンのような負の数）はそのまま書きます。

#### `summary.csv`（複数経路 ／ **1 行 = 1 経路**）

| 列 | 単位 | 内容 |
| --- | --- | --- |
| `id` | — | 経路 ID（入力 CSV の `id`） |
| `status` | — | 判定（`OK` / `NG` / `ERROR`） |
| `freq_mhz` | MHz | 周波数 |
| `gain_tx_dbi` | dBi | 送信アンテナ利得 |
| `gain_rx_dbi` | dBi | 受信アンテナ利得 |
| `h_tx` | m | 送信側アンテナ高 |
| `h_rx` | m | 受信側アンテナ高 |
| `rx_dbm` | dBm | 受信レベル |
| `margin_db` | dB | マージン（受信レベル − 受信感度） |
| `fspl_db` | dB | 自由空間損失 |
| `diff_db` | dB | 回折損 |
| `veg_db` | dB | 植生減衰 |
| `env_db` | dB | 環境損失 |
| `rain_db` | dB | 降雨減衰 |
| `gas_db` | dB | 大気減衰 |
| `total_loss_db` | dB | 総損失 |
| `slant_m` | m | 斜距離（整数） |
| `f1_pct` | % | F1遮蔽率（**100% で頭打ち**） |
| `note` | — | 入力 CSV の備考（自由文字列） |
| `error` | — | 失敗の理由。成功した経路では空 |
| `f1_depth_x` | ×F1 | F1侵入深さ＝障害物が F1 半径の**何倍**まで食い込んでいるか（**頭打ちしません**）。`f1_pct` が 100 のとき、`1.00` なら*ちょうど*完全遮蔽、`2.50` なら F1 半径の 2.5 倍まで突き抜けています |
| `samples` | 点 | その回線で地形を刻んだ点数。**解像度の段階と経路から行ごとに決まる**ので、同じ CSV でも行によって違います。⚠️ **標本は等間隔ではありません**（「高」「中」は DEM 画素の縁ごとに置きます）＝**点数から間隔は求められません**。効いている刻みは帳票の「結果の取扱に関する補足」に出る 1 画素の寸法です |

⚠️ **`status` が `ERROR` でも数値列が埋まっていることがあります**＝計算は通り、成果物（グラフ・レポート）の生成だけが失敗した場合です。何が欠けたかは `error` 列が持ちます。

#### `hops.csv`（中継経路 ／ **1 行 = 1 区間**・N 行で 1 経路）

| 列 | 単位 | 内容 |
| --- | --- | --- |
| `group_id` | — | 経路 ID（同じ経路の全区間で同じ値） |
| `hop_index` | — | 区間の番号（1 始まり） |
| `hop_id` | — | 区間の ID |
| `from` | — | 区間の始点の地点名 |
| `to` | — | 区間の終点の地点名 |
| `status` | — | 区間の判定（`OK` / `NG` / `ERROR`） |
| `freq_mhz` | MHz | 周波数 |
| `gain_tx_dbi` | dBi | 送信アンテナ利得 |
| `gain_rx_dbi` | dBi | 受信アンテナ利得 |
| `h_tx` | m | 送信側アンテナ高 |
| `h_rx` | m | 受信側アンテナ高 |
| `rx_dbm` | dBm | 受信レベル |
| `margin_db` | dB | マージン |
| `slant_m` | m | 斜距離（整数） |
| `f1_pct` | % | F1遮蔽率（**100% で頭打ち**） |
| `error` | — | 失敗の理由。成功した区間では空 |
| `f1_depth_x` | ×F1 | F1侵入深さ＝障害物が F1 半径の**何倍**まで食い込んでいるか（**頭打ちしません**）。`f1_pct` が 100 のとき、`1.00` なら*ちょうど*完全遮蔽、`2.50` なら F1 半径の 2.5 倍まで突き抜けています |
| `samples` | 点 | その区間で地形を刻んだ点数。**解像度の段階と区間の長さから区間ごとに決まる**ので、同じ経路でも区間によって違います |

⚠️ **区間をまたいで損失は足しません**（再生中継＝受けて送り直すため）。全体判定は最もマージンの小さい区間のもので、**判定できなかった区間（ERR）が 1 つでもあれば全体も ERR** です。

#### `scenario.csv`（条件探索 ／ **1 行 = 1 条件**・スイープでは 1 点）

| 列 | 単位 | 内容 |
| --- | --- | --- |
| `label` | — | 条件名（スイープでは軸の値） |
| `condition` | — | ⚠️ **2 列目だけは名前が変わります**＝スイープでは軸の名前（`freq_mhz` など）になり、値はその軸の値です。比較では `condition` のまま、値は条件名です |
| `status` | — | 判定（`OK` / `NG` / `ERROR`） |
| `rx_dbm` | dBm | 受信レベル |
| `margin_db` | dB | マージン |
| `total_loss_db` | dB | 総損失 |
| `fspl_db` | dB | 自由空間損失 |
| `diff_db` | dB | 回折損 |
| `veg_db` | dB | 植生減衰 |
| `env_db` | dB | 環境損失 |
| `rain_db` | dB | 降雨減衰 |
| `gas_db` | dB | 大気減衰 |
| `f1_pct` | % | F1遮蔽率（**100% で頭打ち**） |
| `slant_m` | m | 斜距離（整数） |
| `freq_mhz` | MHz | その条件で使った周波数 |
| `p_tx_dbm` | dBm | 送信出力 |
| `gain_tx_dbi` | dBi | 送信アンテナ利得 |
| `gain_rx_dbi` | dBi | 受信アンテナ利得 |
| `sens_dbm` | dBm | 受信感度 |
| `h_tx` | m | 送信側アンテナ高 |
| `h_rx` | m | 受信側アンテナ高 |
| `veg_h` | m | 植生高 |
| `rain_mmh` | mm/h | 降雨強度 |
| `env_type` | — | 環境区分 |
| `diff_method` | — | 回折モデル |
| `f1_depth_x` | ×F1 | F1侵入深さ＝障害物が F1 半径の**何倍**まで食い込んでいるか（**頭打ちしません**）。`f1_pct` が 100 のとき、`1.00` なら*ちょうど*完全遮蔽、`2.50` なら F1 半径の 2.5 倍まで突き抜けています |

⚠️ **軸に `freq_mhz` / `h_tx` / `h_rx` / `veg_h` を選ぶと、同じ名前の列が 2 つ出ます**（2 列目＝軸、後ろ＝その条件で使った値）。列を名前で引く読み方だと後ろの列が勝つので、**2 列目は位置で読んでください**。

> 📣 **予告（3.1 で直します）**: この重複は**こちら側の落ち度**なので、**3.1 で 2 列目の列名を固定の名前に変え、同じ名前が 2 回出る形をやめます**（軸の名前は別の場所で持ちます）。新しい列名は 3.1 の CHANGELOG で示します。
> - **2 列目を位置で読んでいる場合は、そのままで影響ありません。**
> - **列を名前で引いている場合は、3.1 で 1 度だけ見直しが要ります**（いま `condition` で引いている比較モードの読み方も含みます）。
> - ⚠️ **3.0 では何も変わりません**（この版は予告だけです）＝変更規約 2「**削除・改名・意味の変更は1 つ前の版で予告してから**」に従っています。

#### `terrain_profile.csv`（個別シミュレーション ／ **1 行 = 地形断面の 1 点**）

| 列 | 単位 | 内容 |
| --- | --- | --- |
| `Distance_m` | m | 送信点からの水平距離（0.1 m 刻み） |
| `Elevation_m` | m | 標高（地球曲率の補正を掛ける前の生の値・0.01 m 刻み） |

⚠️ **行数は、その実行で地形を刻んだ点数そのものです**（解像度の段階と経路で決まります）。

⚠️ **「高」「中」では階段状に並びます**——標高データは 1 画素の中では一定の値なので、経路が通る画素ごとに**その画素に入る位置と出る位置の 2 行**が入ります。距離は 0.1 m 刻みで書くため、**同じ距離の行が 2 つ隣り合って見えることがあります**（標高が違えば、それが画素の境目です）。「低」は従来どおり等間隔に並びます。

---

## アンインストール

1. アプリが起動していないことを確認します
2. 展開したフォルダをまるごと削除します

レジストリや AppData への書き込みは行っていません。フォルダ削除のみで完全にアンインストールできます。

---

## 既知の制限・注意事項

### 精度

- DEM の水平解像度（5〜10m）が伝搬計算精度の上限です。建物単体の遮蔽は考慮されません
- Bullington 法は複数障害物の近似であり、**障害物が 1 つの経路では**±5〜15 dB 程度の誤差があります（**複数の障害物が重なる経路の合成損失は、この範囲では語れません**）
- 🔴 **回折損は過大にも過小にも出ることがあり、どの経路で信頼できるかを事前に見分ける方法がありません。** 3.0 で回折モデルを **Bullington 等価ナイフエッジ（ITU-R P.526 §4.5.1）** に替え、①山越えで数百〜数千 dB になる**発散**と、②**植生高やアンテナ高を 1m 動かすだけで 84 dB 跳ぶ不連続**（アンテナを 1m 上げたら回折損が 84 dB 増える形）の 2 つを解消しました。ただし**外れる可能性そのものが消えたわけではありません**＝①合成損失は、系統の違う 2 手法（Epstein-Peterson 法・従来の独自実装）と並べて桁を確かめただけで、**実測とも基準実装とも突き合わせていません**。②**離れた 2 つ以上の尾根がある経路では小さめに出ます**（この手法の既知の性質で、標準もそれを承知で採っています）。③**単一の丘では、標準の補正項のぶん従来より大きめに出ます**。④**地形の解像度を上げると回折損が増えます**（「低」の 20m 間隔 →「高」の画素の縁ごとで最大 +407%）。⚠️ ただし**「高」の中でさらに細かく刻んでも動きません**＝DEM の標高は画素の中で一定の階段状の場なので、**画素の縁に標本を置いた時点で「刻みの細かさ」という自由度そのものが消えます**（実測＝25 万点まで刻んだ収束値 27.014 dB に対し、画素の縁 210 点で 27.02 dB）。⑤**平滑地球の球面回折の項を持ちません**＝**低いアンテナで見通し距離を超える長い平坦路**（海上・干潟など）では過小になりえます。⚠️ 代表 26 経路では 1 本も該当しませんでしたが、**この製品はその条件を検査していません**。⚠️ **起伏の大きさでも F1 遮蔽率でも、どの経路が外れるかは判定できません**（起伏は予測子ではありません）。⚠️ **F1 遮蔽率は画面・レポート・CSV とも 100% で頭打ち**（内部の生値は 100% を超えます）なので、率の側からは異常に気づけません。
- 🛡 **上の限界に対する自衛策**＝**「回折モデル」を `Single` に切り替えてもう一度計算し、2 つの値を見比べてください。**大きく食い違う経路では、**既定（Bullington）の値を信用しないでください**——判定が NG でも、**その NG が正しいかどうかはこの値からは分かりません**。⚠️ **これは「どちらが正しいか」を決める手段ではありません**＝2 つの差が大きければ**結果がモデルの選び方に強く依存している**と分かる、という診断です。**差が小さくても正しさは保証されません**（どちらの値も実測や基準実装と突き合わせていないため）。⚠️ `Single` は**複数障害物の合成損失を表現せず**（最大の 1 点しか見ない）、**標準の補正項も掛けない**ので、**Bullington より小さく出ます**。⚠️ これも**真の値との関係は分かりません**（「`Single` 側が安全寄り」という意味ではありません）
- 植生減衰モデルは経験的近似です。樹種・密度・季節変動は考慮されません
- 環境損失係数は経験値であり、特定地域・用途への適合は保証されません

### データカバレッジ

- **本ツールのDEMは日本国内限定です。** 国土地理院タイルは日本国外をカバーしないため、日本国外の座標を入力すると標高 0m が返ります
- `dem5a_png` / `dem5b_png`（5m）は全国カバーではありません。未整備地域は自動的に `dem_png`（10m）にフォールバックします
- 海上・湖沼・データ欠損箇所は標高 0m として扱います

### 距離

- 実用上は 20 km 以内を推奨します。それを超える距離はスクリーニング用途の精度限界を超えます

### 操作

- グラフウィンドウを開いている間は同じルートのパラメータ変更ができません
- 地形キャッシュはアプリ再起動でリセットされます（ディスクキャッシュはセッションをまたいで有効です）
- 🔴 **表示スケールを変更したあとは、RadioSim を再起動してください**（Windows の「拡大縮小とレイアウト」を**アプリの起動中に**変えた場合）。再起動せずに**ランチャー**をドラッグすると、**ドラッグしている間ランチャーの大きさが少しずつ変わり続けます**（倍率を上げたあとは縮み、下げたあとは広がります）。⚠️ **再起動すれば正しい大きさになります**。⚠️ **起きるのはランチャーだけです**——大きさを変えられるウィンドウ（地図・複数経路・条件探索・中継経路・グラフ）では起きないことを実測で確認しています。**ランチャーを動かさなければ、そのまま作業を続けられます。**⚠️ 起動したあとにスケールを変えていなければ起きません（複数モニタの併用そのものは問題ありません）。⚠️ これは**土台にしている GUI ツールキット（Tk 8.6）側の問題**で、アプリからは回避できないことを実測で確認しています（10 通り以上の回避策を試して、いずれも効きませんでした）。⚠️ **新しい Tk（9.0）では起きないことを実測で確認しました**が、乗り換えには他の条件（土台にしている Python の対応）が要るため、時期は未定です
- 表示スケールの違うモニタを併用しているとき、ウィンドウを別のモニタへ移すとアプリ全体の文字がその倍率に揃います。ただし**メニューバーの帯（ファイル / 設定 / ヘルプ）だけは Windows が描く**ため、そのウィンドウが載っているモニタの倍率のまま残り、他より小さく（または大きく）見えることがあります。表示だけの問題で、操作には影響しません

---

## 著作権

© 2026 BearValley AI Craftworks. All rights reserved.

本ソフトウェアは **MIT ライセンス**で配布しています。ライセンス本文は `RadioSimPro.exe` と同じフォルダの `LICENSE` にあります。商用利用も可能です。
