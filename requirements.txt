# Runtime dependencies (see README for details).
# Pinned to known-good versions for reproducible CI runs and binary builds —
# an unpinned upstream bump (e.g. numpy/matplotlib) can silently break the
# packaged app that ships to users. To refresh: bump a pin, run the full test
# suite + a build, then commit. Transitive deps are not locked here; add a
# full lockfile only if bit-exact reproducibility becomes necessary.
# ⚠️ numpy は 2.4.4 で据え置き（2026-07-30 判断）。2.5.x は Requires-Python>=3.12 で、
# 上げると宣言している対応 Python の下限が 3.11 → 3.12 に動く＝ソース実行の利用者に
# 対する契約変更になる。取り込みたい機能・修正が無いので下限を守る側を選んだ。
# 下限を上げるときは次の 4 ファイル 6 か所を同時に直すこと（2026-09-12 に数え直し）＝
# pyproject.toml（コメントと target-version）・README.md・docs/developer_ja.md と
# docs/developer_en.md（それぞれ 2 か所）。引き上げは 4.0 の予定（ロードマップ §3.4）。
# 間接依存の contourpy も 1.4.0 から Requires-Python>=3.12 だが、ここでは固定しない
# ＝3.11 では pip が 1.3.x を選ぶので、宣言している下限は動かない。
numpy==2.4.4
matplotlib==3.11.2
requests==2.34.2
Pillow==12.3.0
sv-ttk==2.6.1
darkdetect==0.8.0
markdown==3.11
truststore==0.10.4
tkintermapview==1.30
