# experiments/ — 判断の根拠を再実行できる形で残す置き場

**製品コードではない。** ここのスクリプトはアプリのどのモジュールからも import されず、
配布物（`radiosim.spec`）にも含まれない。CI の型検査・カバレッジの対象外（`ruff` だけは
リポジトリ全体を見るので構文と体裁は保つ）。

## なぜ残すのか

設計判断の根拠が「あのとき計算した」という記憶だけになると、**後から前提を変えて
再判定できない**。判断を覆す条件（周波数・アンテナ高・距離）を入れ直せる形で置いておく。

## 一覧

| スクリプト | 何を判定したか | 実行日 |
|---|---|---|
| [phase0_two_ray.py](phase0_two_ray.py) | 2 波干渉（地面反射）が対空リンクで無視できるか＝**段階 0（机上判定）**。結論＝棄却できない／効くのは GCS 前方 3〜80m の地表／ヌルの「位置」は予測不能で予測すべきは振幅の包絡線 | 2026-07-26 |
| [phase0_survey_accuracy.py](phase0_survey_accuracy.py) | **離陸点の測量精度 ±N m** が余裕度を何 dB 動かすか＝RadioSim Tracer の精度目標の逆算。ゴールデンコーパス 26 本（かすめ帯を含む）から再計算するのでネットワーク不要。結論＝回折が効く回線の勾配は **1.3〜1.7 dB/m**・見通し回線は 0 dB/m ⇒ **±0.25 m（目標 0.3 dB）／±0.65 m（上限 1.0 dB）**＝単独測位 GNSS では 1〜2 桁足りず、離陸点で一度だけ RTK 級が要る | 2026-08-02 |
| [font_fallback_probe.py](font_fallback_probe.py) | 日本語の書体が本当に揃っているか＝`font actual … -- 漢` で**実際に描かれる書体**を測る（バッチ表の見出しだけ別書体になる不具合の調査用）。結論＝Segoe UI Variable 系は漢字をフォントリンクに逃がし、太字では Malgun Gothic（韓国語）へ落ちる／ja の本文を Yu Gothic UI にすると全用途で一致 | 2026-07-31 |
| [b119_frame_slip_probe.py](b119_frame_slip_probe.py) | **別 DPI のモニタへ移した窓が 6px ずつ縮み続けるのは誰の仕業か**。`wm geometry` を呼んだ者を呼び出し元つきで記録するので、**Python が書いたのか Tk/WM が縮めたのか**が 1 回で分かる。結論＝**暴走中に `wm geometry` は 1 行も出ない**＝縮めているのは Tk/WM でアプリは無関与／ずれ量は装飾幅の差（96↔144 で 6px）と一致し向きも対称／**ドラッグ中かどうかは無関係**（手を離しても続く）。⛔ **「Tk が移る前の枠の厚みを持っている」という説明は `rebuild` で棄却済み**（変更後に建てた窓は枠の値が正しいのに同じく刻む）＝**原因は未解明**で、棄却できた候補 10 通り超が docstring に在る | 2026-08-23 |
| [b119_resizable_windows_probe.py](b119_resizable_windows_probe.py) | **表示スケール変更後の寸法ドリフトが「リサイズできる窓」（バッチ・条件探索・中継経路・グラフ・地図）にも起きるか**＝ランチャーは `resizable(False, False)` なので未確認のまま残っていた面。開いている全トップレベルを走査して寸法の変化を記録し、**中身も寸法も同じでリサイズ可否だけ違う対照 2 枚**（`Ctrl+Shift+B`）を並べて可否を独立変数として切る。⚠️ **モニタ 1 枚で足りる**（スケールを実行中に変えるだけ） | **未実施** |
| [tk9_dpi_probe.py](tk9_dpi_probe.py) | **Tk 9 移行の再判断トリガー②③の実測**＝表示スケールを変えた前後で ①フォントの実効サイズが**アプリが何もせずに**変わるか（③フォント追従）②表示スケールを変えたあとのドラッグで窓が 6px ずつ変わり続ける現象が消えるか、を測る。**サードパーティを 1 つも import しない**＝cp315 の wheel が揃っていなくても③の答えが出る。`--no-drag` で Tk 8.6 側と**同時に**回せる（人のスケール変更 1 回で 2 インタプリタぶん採れる）。正典＝メモリの [[project-tk9-migration]] | 2026-08-23 |
| [b032_deygout_probe.py](b032_deygout_probe.py) | **Deygout の多重回折が広がった障害物で発散する問題の現状を測る**＝合成地形（平地・広い丘・細い峰・2 つの峰・一様キャノピー）で `single` と `deygout` を並べ、**再帰回数と ν の幅**も採る。結論＝発散の機構は「**区間を割るほど `d1·d2/(d1+d2)` が縮んで ν が人工的に増える**」＝`Single` が見通しと判定する ν=-0.79 の丘が、割った先で ν=-0.06 になる | 2026-08-25 |
| [b032_variants_probe.py](b032_variants_probe.py) | **その発散に対する候補処方を同じ土俵で並べる**＝端点・ν の基準・Causebrook 減衰・深さ上限・陰ゲート・一体化の切り方・分離度重みの 7 軸。**落ちた案も残す**（頂点端点＝平地 227 dB／全経路 ν＝平地 67 dB／Causebrook＝広い丘 80m で 1020 dB／陰ゲート＝多重回折が消える／分離度重み＝合成では全条件を通ったが**実地形で 1557 dB**）。✅ 結論＝**Kν＝「ν > −0.8 が連続する範囲は 1 枚のナイフエッジ」だけ**。🔑 **θ は「LoS 超過（0.0）〜 F1 の 57%（−0.8）」の連続したつまみ**で、**−0.8 でだけ不連続な跳びが消える**（一体化の境界と損失の有無が一致するため。−0.2〜−0.4 では hiroshima が 39↔124 dB と跳ぶ）。⚠️ **`_NU_THRESHOLD = −0.8` は F1 の 57% クリアランス＝実務の「60% ルール」そのもの**なので、これは標準と同じ物差しで切っているということ。⚠️ **合成の「2 峰」は既定のアンテナ高では谷が F1 の 43% しかクリアせず電波的に独立していない**＝一体化されるのが正しい。**本当に独立した 2 峰（`独立2峰(高AT)`）では Kν は Single の 2.06 倍を出す** | 2026-08-25 |
| [b032_golden_probe.py](b032_golden_probe.py) | **候補処方を実データで測る**＝`tests/data/golden_links.json` は標高配列を凍結しているので**ネットワークに触らず**26 本を再計算できる。見るのは①回折損②受信レベル③**OK/NG が変わる本数**。⚠️ **`earth_k` は渡さない**（`k_factor` はライス K で曲率とは無関係＝Codex 48 巡目 P1 で踏んだ）。⚠️ **自分の主張の反例を対象から外さない**（Codex 49 巡目 P2 で踏んだ＝表を絞ったら「θ=−0.8 は安定」の反例が落ちていた）。結論＝Kν は**判定 0 本変化・100 dB 超が 13 本 → 2 本**・hiroshima の標本数依存が 1860→2325 dB から **38.8→39.2 dB** へ。ただし**深い遮蔽（`veg_low_antenna`）では +18% 残る**（現行 +92%） | 2026-08-25 |
| [b032_reference_probe.py](b032_reference_probe.py) | **候補処方の値が妥当かを系統の違う手法と照らす**＝Bullington（P.526 §4.5.1・補正項つき）と Epstein-Peterson を独立に実装して並べる。⚠️ **真値ではない**。🔑 **この照合が処方を 3 度変えた**＝①陰ゲート案は障害物 1 つの回線でも 3 手法一致値の 2 倍 ②一体化＋陰ゲートは逆に Bullington より下 ③**手法の大小に一般的な順序は無い**（fuji では Bullington < Ep-Pet < Kν、hiroshima では逆）＝1 行から一般化しない | 2026-08-25 |
| [b032_terrain_probe.py](b032_terrain_probe.py) | **「なぜこの回線で Deygout が `Single` と同じ値になるのか」を地形の断面で確かめる**＝標高・LoS・F1 半径・ν・一体化の範囲を 1 枚の表に出す。結論＝`hiroshima_kure_ridge` は **9km 連続の山塊**で、谷でも LoS 差 +31〜42m（F1 半径 12〜24m の 1.3〜1.8 倍**上**）＝電波は一度も回り込めない ⇒ **1 枚と見るのが物理的に正しい**。Ep-Pet が「障害物 3 つ」と数えたのは **LoS をわずか数 m 下回る点が 3 か所**あるためで、F1 半径に対して無意味な凹み | 2026-08-25 |
| [i069_resolution_probe.py](i069_resolution_probe.py) | **解像度の段階（高/中/低）ごとに、実データで答えがどれだけ動くか**＝コーパス 26 本を 3 段階で回し、現行の点数と判定・受信レベル・回折損を突き合わせる。🔑 **測ったから 2 つ決まった**＝①**旧上限 2000 が段階そのものを潰していた**（「高」が 12 本で張り付き・6 本は高と中が同じ答え・48km の 1 本は 3 段階すべて同じ）②**上限は性能上の必然ではない**（48km で N=240 が 0.3 秒・N=9677 でも 0.8 秒＝コストは距離で決まる）。判定は 3 本が OK → NG（うち 1 本は現行 220 点が尾根を跨いでいた是正） | 2026-08-25 |
| [review_benchmark/](review_benchmark/) | **独立レビューのベンチマーク**＝Codex が実際に出した指摘 11 件（全件真）＋対照の偽陽性 1 件に、裏取り済みの正解ラベルを付けたもの。**レビュアーを入れ替えたとき（ローカル LLM に託せるか等）に同じ物差しで測る**ため。コード面は git で完全再現・文書面は復元手順つき | 2026-07-26 |

実行:

```
python experiments/phase0_two_ray.py
```

⚠️ **`b119_*_probe.py` の 2 本と `tk9_dpi_probe.py` だけは例外**＝GUI を建てて**人が表示スケールを変え、窓を
ドラッグする**探針で、実機でしか意味を持たない（それ以外は純計算＝ネットワーク・GUI・
ファイル書き込みなし）。⚠️ 2 画面は要らない＝**1 枚でスケールを変えれば再現する**
（デュアル DPI 前提は 2026-08-23 に棄却済み）。結論は課題台帳の該当項目が正典。

物理・設計判断の側（`phase0_*`）は、結論の全文とそこから決まった設計方針を
memory の `project-radiosim-for-drone` / `project-radiosim-tracer` が持つ。
