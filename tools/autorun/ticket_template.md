---
issues: B-nnn I-nnn
accept: tests/test_example.py
accept: tests/test_claude_hooks.py -k Example
scope: core/example.py tests/test_example.py
max_turns: 80
---
<!--
作業票の雛形（無人の駆動＝tools/autorun/run.ps1）。1 枚＝1 ステージ＝1 コミット。
置き場＝.claude/tickets/<版>/<2 桁の順番>-<短い名前>.md（名前の順に回る）。追跡外＝票は課題 ID を持つ（台帳と同じく非公開）。
駆動スクリプトは作業ツリーがきれいでないと始めない（追跡外の票は数えない）。

頭の 4 項目（駆動スクリプトが読む）:
  issues    … 課題 ID（空白区切り・B-nnn の形）。中身は写さない＝作業員が ledger.py で取る
  accept    … 受け入れのテスト＝`python -m pytest -q` に渡す引数。1 行 1 本・何行でも
              作業員が終えた後、駆動スクリプトが自分で回す（合否はここで決まる）
  scope     … 変えてよいパスの前方一致（空白区切り）。コミットの差分がこの外に出たら赤
              台帳（ISSUES.md）とメモリはコミットに入らない＝ここに書かなくてよい
  max_turns … 1 セッションの API 往復の硬い上限（省略時は駆動スクリプトの既定）

下の本文は作業員へそのまま渡る。書くのは受け入れ条件と、やってはいけないことだけ。
⛔ 判断が要る票は無人に向かない（画面の撮影・本物のインストーラ・途中の選択・リリース）。
-->

## 受け入れ条件

- （例）`tests/test_example.py` に元の形を仕込むと落ちるテストがある
- （例）台帳の対応欄にコミットのハッシュと何をしたかを書く
- （例）版計画 §X.Y のステージ行は ⬜ のまま（✅ にするのはマージした人）

## やってはいけないこと

- （例）`core/` の公開関数の引数を変える
