# CHANGELOG — RadioSim Pro

形式: [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 準拠

粒度: **1 版 = 1 節**。ベータ／RC は配布形態の検証段階であって版ではないため、独立した節は設けず、正式リリース時に該当版の節へ集約する。

---

## [2.8] — 未リリース（ベータ）

テーマ「利用者の手に開く版」。**芯は機能追加 2 件**（中継点を任意の位置に挿せる／表示言語を自分で足せる）で、残りは**そのついでの一貫性の回収**。2.7 と同じく、この節に載るのは**画面の振る舞いが実際に変わるもの**だけ。

### 追加

- **中継経路の地点表に `＋` が付き、任意の位置に中継点を挿せるようになった**。押した行の**下**に中継点が入る。従来は「受信点の手前」にしか足せず、3 点目の次に 1 点入れたければ**後ろの地点を消して打ち直す**必要があった（削除は 2.7 から任意の位置でできていたので、足す側だけが不揃いだった）。⚠️ **受信点の行には `＋` は出ない**（末尾は受信点で固定のため）。⚠️ **地点の名前は振り直さない**（`R1` を「東の丘」に直した入力を上書きしないため）。挿すと、その地点から出ていく区間が**空欄で 1 つ増える**（同じ位置で挿して消せば元どおり）。
- **表示言語を自分で足せるようになった**。アプリと同じフォルダに `lang` フォルダを作り、`<言語コード>.json` を置いて再起動すると、**設定 > 言語**のメニューに現れる。`_name` にその言語自身の字で名前を書く。**訳が無いキーは英語のまま**出るので、全部を訳さなくても壊れない。⚠️ **合わない行（`{n}` のような差し込みを落とした訳など）は採用せず、起動時にどのファイルの何件を採らなかったかを画面で知らせる**（黙って壊れたまま動かさないため）。⚠️ **追加した言語では画面の見切れを保証しない**——ウィンドウの寸法は日本語と英語の語の長さで検査してあるため。**非公式な訳**という位置づけ。⚠️ **レポート・CSV など成果物の語は対象外**（出力の字が利用者ごとに変わらないようにするため）。

### 変更

- 🔤 **配布物に同梱しているマニュアルのファイル名が変わった**＝`README_binary_ja.md` / `README_binary_en.md` → **`docs/manual_ja.md` / `docs/manual_en.md`**。ヘルプメニューから開く動きは同じ。あわせて開発者向けの `README_ja/en.md` も `docs/developer_ja/en.md` になった（リポジトリ直下に README が 5 つ並んでいたのを、**入口 1 本 ＋ `docs/` の 4 本**へ整理したもの）。
- 🔤 **ヘルプの「READMEを開く」を「ドキュメントを開く」に改めた**（英語表示は `Open Documentation`）。開く実体がファイル名で呼べなくなったため、**実行形態で語を変えない上位語**に寄せた。
- **複数経路の実行の帯から、判定の集計（`✓ 3 OK  ✗ 1 NG  ⚠ 0 ERR`）を外した**。2.7 で**行ごとの判定列**が入って以降、帯の集計は同じ事実の数え直しになっていた（判定は行が持ち、帯は進捗だけを言う）。⚠️ **数え方も判定そのものも変えていない**——落としたのは表示の重複だけ。
- **複数経路の経路 ID に打てる長さを 16 文字から 11 文字にした**。従来は 16 文字まで受け付けるのに**欄は 9 文字ぶんしか見えず**、`asaminami24` が `asaminami2` に読めていた。**打てる長さと読める長さを揃えた**もの。⚠️ **大文字が続く ID は 7 文字程度が上限**（`W` や `M` は数字の 1.8 倍の幅があるため）。
- 🔤 **共通設定のラベルの括弧を全角に統一した**（`周波数 (MHz)` → `周波数（MHz）`）。主画面は全角・共通設定は半角と、同じ語が窓によって違う字で出ていた。

### 修正

- 🔴 **子ウィンドウを別の表示スケールのモニタへ移すと、そのウィンドウだけ字の大きさが追従しなかったのを直した**。**移した先のモニタに合わせてアプリ全体の字が揃う**ようになる。⚠️ **ウィンドウごとに別々の大きさにはできない**（フォントはアプリに 1 組しか持てないため）。
- **表示スケールを 100% → 150% → 100% と戻したとき、ウィンドウの幅だけ 12px 広いまま残っていたのを直した**。
- **中継経路で、地点の名前を変えても区間表の見出しが古い名前のまま残っていたのを直した**（数値は正しく、同じ窓の中で 2 つの表が違う名前を名乗っていた）。
- **中継経路で、入力を変えても前の区間結果が残っていたのを直した**。地点・区間・共通設定のどれかが変わると、その結果は消える（2.7 で複数経路に入れたのと同じ規律を中継にも通した）。⚠️ **値が変わらないキー操作では消えない。**
- **ランチャーの座標欄だけが、他の窓と違う幅で表示されていたのを直した。**

## [2.7] — 2026-08-11

テーマ「器の手入れ」。**利用者への約束（出力の中身・保存先・配布形態）は変えず**、土台を整える版。したがってこの節に載るのは、下の 1 件のように**画面の振る舞いが実際に変わるもの**だけになる。

### 変更

- **実行結果は、その結果を生んだ入力の行に返るようになった**。複数経路の表には**判定列**が増え、実行中に上から順に `OK / NG / ERR` が入る（どの経路が NG かを見るためにレポートを開く必要がなくなった）。中継経路は同じ理由で**区間の表**に受信レベル・マージン・判定が入り、**別置きの結果一覧は廃止**した（全体判定は残る）。結果一覧が無くなったぶん中継経路の窓は約 210px 低くなり、地点を 8 個置いても画面（フル HD）に収まる（従来は 5 個で頭打ち）。
- **成果物（断面図・詳細レポート・KML）を作れなかった経路を「成功」に数えなくなった**。従来は計算さえ通れば OK/NG に数えており、図やレポートが 1 つも作られていなくても `⚠ 0 ERR` で完了していた。今後は判定が `ERR` になり、台帳（`summary.html` / `summary.csv`）・`summary.kml` でもエラーとして現れる。**計算できた数値は台帳に残る**（欠けたのは成果物だけなので消さない）。
- **実行の帯（状態の 1 行・進捗バー・「実行」ボタン）を 4 つの窓で同じ形に揃えた**。状態の表示は**どの窓でも進捗バーのすぐ上・左寄せの 1 行**になる（従来は複数経路だけがこの形で、条件探索と中継経路はバーの横、ランチャーは中央寄せ＝4 窓で 3 通りだった）。バーの横に置いていた窓では、状態の文言が伸びるほど**進捗バーが短くなって**いたが、これも起きなくなる。⚠️ **揃えたのは場所であって中身ではない**（`OK / NG / ERR` の集計は、複数本を続けて回す窓にだけ出る）。あわせて**複数経路の進捗バーが「実行」ボタンまで届いていなかったのを直した**（バーとボタンのあいだに進捗の件数表示が挟まっており、この窓のバーだけが短かった）。件数は状態の行へ移した。
- **「実行」ボタンの大きさを 4 つの窓で揃えた**。複数経路だけがボタンの幅を広く取っており、同じ「実行」という文字が窓によって違う大きさに見えていた。主操作であることは**位置（帯の右端）と色**で表す。
- **地形断面画面の「保存」ボタンを 1 行の高さにし、操作欄の右下へ置いた**。従来はスライダー 3 行ぶんの高さを占める縦長のボタンで、他の窓の主操作（1 行）と不揃いだった。
- **座標表記（DD / 度分秒）の切り替えは、窓を開いた時点の設定で固定される**ようになった。開いたままの複数経路画面や地形断面画面には、あとから設定を変えても反映されない（反映するにはその窓を開き直す）。アプリ全体の方式（**窓は開いた時点の値で凍結する**）に、アプリ設定も合わせたもの。
- 🔤 **窓の名前を「何をする場所か」で言い直した**。**「一括シミュレーション」→「複数経路」**（`中継経路` と対になる＝複数経路 = N 本の独立した回線／中継経路 = 1 本の内訳。「シミュレーション」は 4 つの窓すべてがやることなので、名前として効いていなかった）、**「マップウィンドウ」→「地図」**（「ウィンドウ」が付くのはこれだけだった）。条件探索・中継経路はそのまま。⚠️ **レポートの中の「一括シミュレーション」は変えていません**——保存された成果物の字は読む側との約束なので、画面の都合で動かさない（画面とレポートで名前が食い違うのは承知のうえで、揃えるのは出力の版でまとめて行います）。
- 🔤 **ランチャーの 4 つのボタンを「何を回すか」の順に並べ替えた**。1 行目が `複数経路 | 中継経路`（どちらも経路を複数回す親戚）、2 行目が `条件探索 | 地図`。**地図は他の 3 つの入力元**なので最後に置いた（実行するボタンと同じ列に並ぶと同格に見えるため）。見出しや区切り線は足していない。
- 🔤 **条件探索の「固定した経路」を「経路」に改め、中身を「送信座標」「受信座標」の 2 欄に分けた**。従来は `34.5…, 132.4… → 34.5…, 132.4…` の 1 欄で、他の窓に無い表記だった。値が固定されていることは、これまでどおり右端の 🔒 が伝える。
- 🔤 **中継経路の地点表の「地上高(m)」を「アンテナ高(m)」に統一**した。同じ量をランチャーは「送信アンテナ高」、レポートは「アンテナ高」と呼んでおり、**同じ窓の入力と出力で語が違って**いた。⚠️ 表の列見出しの `送信高` / `受信高` は幅の都合による短縮形なのでそのまま。
- 🔤 **英語表示で「経路」を `Path` に統一**した（`Route ID` → `Path ID` ほか 4 か所）。同じ窓の中で入力欄が `Route ID` と名乗り、エラーが `Path ID … is too long` と怒る状態だった。日本語表示は変わらない。
- 🔤 **複数経路の完了表示が、日本語表示でも英語のままだったのを直した**（`Done: batch_20260810_164338` → `完了: batch_20260810_164338`）。実行の帯に出る文言のうち、ここだけが翻訳を通っていなかった。⚠️ **`OK / NG / ERR` は従来どおり**——判定の 3 語は両言語で同じ表記と決めているため、訳しません。
- **中継経路レポートの集約カードは、判定によって語が変わる**ようになった。OK なら従来どおり「全体マージン（最小余裕）」だが、**NG のときは「最大不足」として不足量を正の数で出す**（`−12.4` → `12.4`）。同じ数字でも、OK のときの問いは「あと何 dB 積めるか」、NG のときは「あと何 dB 足りないか」で別物だったため。画面のサマリ 1 行も同じ語に揃えた。⚠️ **数値の出所は変えていません**（最も苦しい区間のマージン）。
- 🔗 **プロジェクトを読み込んでも、開いている窓を閉じなくなった**。従来は「確認のうえ閉じて開き直す」形で、窓の位置・スクロール位置・選択が失われていた。今後は各窓の上に**「プロジェクトを読み込みました。この窓の内容を差し替えられます。」の帯**が出て、**「内容を取り込む」を押したときだけ**その窓の行・条件・地点が入れ替わる。⚠️ **押さなければ画面は変わらない**——このアプリの「窓は開いた時点の値で固定し、見えている値で実行する」という流儀を保つため。取り込まないまま窓を閉じた場合は、**画面に見えていた内容のほうが残る**。⚠️ **既存の「↻ ランチャーから更新」は従来どおり**（共通設定と案件情報だけを取り込み、入力した行や地点には触らない）。
- 🔗 **複数経路の窓から地図を開けるようになった**（「地図から選択」）。押すと地図が「複数経路へ連続追加」モードで開き、TX → RX のペアを置くたびに行が足される。従来この導線は**地図の側からしか始められず**、中継経路の窓には同じ口があるのに複数経路には無い、という不揃いだった。
- 🔗 **中継経路の地点を、任意の位置で消せるようになった**。中継点の行に `×` が付く（送信点・受信点は消せないので `×` は出ない）。従来は「末尾の中継点を 1 つ」しか消せず、3 点目を消すには 4・5 点目も消して打ち直す必要があった。⚠️ **中継点を消すと、その地点から出ていく区間の設定（周波数・利得）も一緒に消える**——残さないと、後ろの区間の設定が 1 つ前へずれて**別の区間の値で計算される**ため。あわせて「− 中継点を削除」ボタンは無くなった。
- 🔗 **中継経路の送信点・受信点に、窓を開いた時点のランチャーの座標が入る**ようになった（中継点は空のまま）。複数経路の表は以前からそうしており、中継だけ空欄から始まっていた。⚠️ **プロジェクトから開いたときは上書きしない。**
- **中継経路と条件探索でも、座標が「設定 > 座標の表示形式」に従う**ようになった。この 2 つの窓だけ常に十進度で表示しており、設定を度分秒にしても変わらなかった。🔴 **あわせて、中継経路が度分秒の入力を受け付けなかったのを直した**（`34°48'00.0"N, 132°36'00.0"E` を入れると「座標は…で入力してください」と拒否されていた）。**正しい座標が拒否される状態**だったが、地図から拾う使い方では表面化しなかった。
- **座標の入力欄を、度分秒でも末尾まで読める幅にした**。従来は十進度にちょうどの幅で、度分秒にすると**末尾の `E` / `W` が画面から消えていた**（値そのものは正しく、欄をスクロールすれば見えた）。⚠️ **表記によって幅は変えない**（切り替えるたびに列がずれないため）。
- **座標を入力して確定すると、その場で現在の表記へ整形される**ようになった（ランチャーと複数経路の表）。度分秒（DMS）表記を選んだ状態で `34.8, 132.6` のような十進度を貼っても表示が何も変わらず、**受理されたのか無視されたのか画面から分からなかった**。今後は確定（Enter / 他の欄へ移動）で `34°48'00.0"N, …` に整形され、**整形されること自体が「読めた」という返事**になる。読めない入力は原文が残る。⚠️ **入力はどちらの表記でも受け付けます**（この設定は表示の話であることが分かるよう、メニューの名前も「座標形式」→**「座標の表示形式」**に変更）。
- 🔍 **Windows の表示スケールを下げると、開いている窓も元の大きさに戻る**ようになった。従来は 150% → 100% に戻しても窓は 150% のときの大きさのままで、**広がる方向の一方通行**だった。⚠️ **手で広げた窓も、スケールを変えた瞬間は既定の大きさに戻ります**（スケールの変更は「窓の大きさが変わる」ことが期待そのものなので、そちらを優先しました）。**解像度が変わっただけのとき（リモート接続の再接続など）は従来どおり縮めません。** あわせて、**表の行の高さが 125% / 150% で必要以上に高くなっていたのを直した**（150% で 55px → 38px。100% での見た目は変わりません）。
- 🔍 **メニューの文字が表示スケールに追従する**ようになった。**帯（ファイル / 設定 / ヘルプ）も、開いた中身（テーマ・言語・座標の表示形式…）も、右クリックのメニューも**対象です。従来は全体が大きくなる中でメニューだけ元の大きさのままだった。⚠️ **帯は Windows が描いており、アプリからは字の大きさを変えられません**——そこで Windows に「非クライアント領域も拡大してよい」と申告する方式（Per-Monitor DPI Aware v2）へ切り替えた。**Windows 10（1703）より前では従来どおり**です。一方**開いた中身はアプリが描いている**ので、こちらは表示スケールに合わせて字の大きさを直接指定するようにした（帯と同じ大きさに揃います）。
- 🔴 **中継経路を開いたときの受信点のアンテナ高が、送信側の値になっていたのを直した**。ランチャーで「送信アンテナ高 30m / 受信アンテナ高 10m」と入れて中継経路を開くと、**受信点も 30m で始まっていました**（送信点は正しく 30m）。気づかずに実行すると受信側を 30m として計算するため、余裕（マージン）が実際より大きく出ます。⚠️ **中継点は従来どおり送信アンテナ高で始まります**（ランチャーに対応する値が無いため）。⚠️ プロジェクトから開いた経路と、すでに編集した高さは変わりません（**窓を開いたときの初期値だけ**の話です）。
- 🔤 **「↻ ランチャーから更新」と「🔒 ランチャーの値」の位置を 3 つの窓で揃えた**（複数経路・条件探索・中継経路とも**「案件情報」の行の右側**）。従来は窓ごとに 3 通りの場所にあり、しかも置き場のせいで窓幅が無駄に広がっていました。⚠️ **押したときの動きは変わりません**（どの窓でも案件情報と共通設定をまとめて取り込みます）。複数経路はこれで 1 行ぶん低くなります。
- **中継経路の窓も細くなった**（**980px → 800px 前後**）。窓の幅が中身と関係なく 980px に固定されていたもの。⚠️ **入力欄・表の中身は何も変えていません**（幅の下限を中身より下げ、帯が幅を決めるのをやめただけ）。
- **条件探索の窓が細くなった**（日本語・十進度で **1105px → 859px**、比較条件を 5 列並べたときは 947px）。右に使われない余白が残っていたのを、窓の幅が**比較条件の列数で決まる**ようにしたもの。座標欄を度分秒でも読める幅にした際、「↻ ランチャーから更新」と「🔒 ランチャーの値」が座標と同じ行で幅を奪い合い、**この帯だけで窓幅を決めていた**ため。⚠️ **この 2 つは「案件情報」の行の右側へ移りました**（どちらも案件情報・経路・ベース列をまとめて扱うものなので、凍結した領域の 1 行目に置いています）。スイープ側も同じだけ細くなります。

### 修正

- 🔴 **プロジェクトを続けて読み込むと、前のファイルの内容が入ることがあったのを直した**。「内容を取り込む」の帯を出したまま、**その窓の情報を持たない別のプロジェクト**を読むと帯が残り、あとから押すと**前のファイルの行・条件・地点**が入っていました（そのまま保存すると 2 つのプロジェクトが混ざったファイルになります）。今後は読み込むたびに、取り込まれていない帯は消えます。
- **実行中に表を編集したとき、古い条件で出た判定がその行に貼られていたのを直した**。実行中も表は編集できますが、判定は**実行に出したときの入力と一致する行にだけ**返るようになりました（編集した行の判定欄は空のままになります）。⚠️ **保存されるレポート・CSV・KML は従来から正しい値です**——計算は実行を押した時点の入力で走るため。直したのは画面の判定欄だけです。

### ドキュメント

- 🔤 **README が、実装に無いボタン名を書いていたのを直した**（`▶ 実行` → **実行**、`個別シミュレーションボタン` → **「実行」ボタン**、英語版の `Save` → **SAVE PACKAGE**、`Relay Route` → **Relay Path**、`↻ Update from launcher` → **↻ From Launcher**）。2.6〜2.7 でボタンを改名した際、画面は直っても文書に古い名前が残っていたもの。⚠️ **画面の動きは何も変わりません**。
- 🔴 **「回折損が当てにならないことがある」ことを README に書いた**。既定の Deygout 法は**回折損を過大に出すことがあり、山越えでは数百〜数千 dB**になります。判定は NG になりますが、**その NG が正しいかどうかも、その値からは分かりません**。⚠️ **どの経路で信頼できるかを事前に見分ける方法はありません**——起伏の大きさでも F1 遮蔽率でも判定できず、**起伏 25m の滑らかな丘でも `Single` 法 0.0 dB に対し 65 dB** が出ます（同じ起伏でも地形の形しだいで 10 倍以上変わります）。
- 📐 **回折モデルの説明を「ITU-R P.526 準拠」から「独自実装」に改めた**。本アプリの Deygout 法が P.526 から使っているのは**ナイフエッジ回折損の式だけ**で、現行 P.526-16 が複数障害物に定める孤立円筒モデルも、一般地形の Bullington 法も、標準の補正項も実装していません。⚠️ **計算そのものは変わっていません**——変わったのは**呼び方**で、準拠と書けるだけの中身が無かったという訂正です。単一エッジ側（`Single`）の Fresnel-Kirchhoff 式とフレネル半径の式は従来どおり P.526 のものです。
- 🛡 **あわせて、今できる自衛策を書いた**＝**「回折モデル」を `Single` に切り替えて計算し直し、2 つの値を見比べてください。**大きく食い違う経路では既定（Deygout）の値を信用しないでください。⚠️ **これは正しさを決める手段ではありません**＝差が大きければ**結果がモデルの選び方に強く依存している**と分かる、という診断です（**差が小さくても正しさは保証されません**）。`Single` は複数障害物の合成損失を表現しない（最大の 1 点しか見ない）ので **Deygout より小さく出ることがあります**が、**それが真の値に近いという意味ではありません**。⚠️ **これは今回作り込んだ欠陥ではなく以前から在ったもの**で、修正は次の大きな版（計算まわりを触る回）で行います。**変わったのは、それを利用者に伝えるようになったこと**です。
- ⚠️ **あわせて「F1 遮蔽率は 100% で頭打ちに表示される」ことを明記した**。画面・レポート・CSV のいずれでも 100% を超えた値は出ません。そのため**上の発散が起きていても遮蔽率は「100%」に見え、率の側からは異常に気づけません**（内部の値は 100% を大きく超えています）。異常を疑う手がかりは**回折損の dB のほう**です。
- **ライセンス（MIT）の本文を配布物に同梱し、4 本の README に明記した**。

### 開発環境

**この版の本体は、画面から見えないここにある。** 上の「変更」は土台を整えた結果として表に出た分で、作業の大半は下の 3 つ。

- **画面層を 3 モジュールに分割した**（振る舞いは 1 つも変えていない）。1 ファイルが大きくなりすぎて、変更の影響範囲を読み切れない状態だったもの。あわせて**層をディレクトリで表す**ようにし（`core/` `report/` `views/`）、依存が一方向（views → report → core）であることと、`core/` が GUI ツールキットや作図ライブラリを引かないことを機械で守るようにした。手書きの層リスト 2 本は消えた。
- **アプリ設定を、窓が直に読むのをやめた**。ランチャーが開いた時点の値を凍結して渡す形に変え、「窓は開いた時点の値で固定し、見えている値で実行する」というこのアプリの流儀を設定にも適用した（利用者に見える変化は上の「座標表記の切り替え」の項）。
- **テストを、開発機の設定と書き込み先から切り離した**。テストが利用者の設定ファイルを読み、リポジトリ直下へログや結果を書いていたため、**既定でない設定の経路を一度も検査していなかった**（実際にこの穴から座標表記の欠陥が出荷されている）。設定・ログ・結果・キャッシュの 4 つの行き先を一時領域へ隔離した。
- **画面の語彙を機械で守るゲートを 3 本足した**。①用語集（`docs/glossary.md`）と実際の文言の突き合わせ ②**画面に出る同じ字を 2 つのキーで持たない** ③**画面に出る自然言語が翻訳の仕組みを迂回していない**（`text=` / `label=` / 窓の題 / ダイアログの 4 経路を検査）。あわせて、**文書が名指しするボタン名が実装に在ること**を検査するようにした——従来のゲートは「実装のラベルが文書に載っているか」しか見ておらず、**改名して古い名前が文書に残った状態は緑になっていた**。
- **窓の後片付けとスレッドの扱いを直した**。窓を閉じたあとに残っていた参照と、破棄済みの窓へ結果を投函する経路を塞いだ（どちらも利用者には見えないが、長く開けたままにすると効いてくる種類のもの）。
- **静的検査を push 前に鳴らすようにした**（型・lint とも、分割作業中に取りこぼしが出たため）。
- `build.bat clean` が、ビルド出力先に置いた配布 zip を消せていなかったのを直した（消す対象の場所は以前に直したが、**削除の順序**のせいで到達しても空だった）。

---

## [2.6] — 2026-08-04

テーマ「中継点」。**1 本の回線を中継点で継ぐ**ための 5 つ目の実行フロー（中継経路）を追加し、あわせて**入力一式を 1 ファイルに束ねる**プロジェクトファイル（`.rsproj`）を導入した。版の頭では、複数の窓にまたがっていた UI の不揃い（実行ボタンの表記・配置、ランチャーのボタン分類、条件探索の情報設計、グラフ窓）を 1 パスでまとめて整えている。

### 追加

- **中継経路**: ランチャーの「中継経路」ボタンで開く専用画面。送信点と受信点のあいだに中継点を最大 7 つ（区間は最大 8 つ）置き、区間ごとに独立したリンクバジェットを計算する。
  - **再生中継（中継点で受けて送り直す）を前提**とし、**区間の損失は足し合わせない**。**全体の可否は最も余裕の少ない区間**で決まる。レポートには全体判定と**区間ごとの内訳の両方**を載せる（どこが苦しいか分からないと、中継点を足すのかアンテナを上げるのかを決められないため）。**反射板（パッシブリフレクタ）は対象外**であることをレポートにも明記する。
  - **高さは地点に、無線諸元は区間に**持たせる。中継点のアンテナは 1 本なので、「前の区間の受信高」と「次の区間の送信高」に別々の値を入れられない構造にしてある。区間ごとに周波数・送受信利得を上書きできる（空欄なら共通設定）。
  - 先頭＝送信点・末尾＝受信点は固定で、追加した地点は**受信点の手前に中継点として挿さる**（並びがそのまま経路の順序）。地図からは 1 点ずつ順に拾える（地図はアプリ唯一のインスタンスのまま・3 つ目の宛先）。
  - 出力は `results/multihop_YYYYMMDD_HHMMSS/` に `route.html`（合成シート）・`report_all.html`（全区間連結）・`hops.csv`（**1 行 = 1 区間**・ファイル名と列名は出力契約なので `hop*` のまま）・区間ごとのパッケージ。`hops.csv` はバッチの `summary.csv`（1 行 = 1 回線）とは**別ファイル**にしてあり、既存の出力契約を壊さない。
- **プロジェクトファイル（`.rsproj`）**: 座標・全パラメータ・案件情報・バッチ行・条件探索の条件・中継経路の地点と区間を **1 つの JSON に束ねて**保存し、あとから再開できる。メニューの **ファイル → プロジェクトを開く / 保存**。
  - **結果とアプリ設定（テーマ・言語・プロキシ）は含めない**。他人のプロジェクトを開いた瞬間に表示言語やネットワーク設定が変わらないようにするためで、読み書きの両側で sim パラメータだけに絞る。
  - **閉じている窓の内容も保存される**（前回値を持ち越す）＝バッチ窓を閉じただけで行が消えたファイルを上書き保存しない。読み込むときは開いている窓を確認のうえ閉じ、**内容はその窓を開いたときに反映される**（「窓を開いた時点の値で凍結する」というアプリ全体の方式に合わせている）。
  - 版の互換性は**ドキュメント全体で 1 つの `schema_version`**。未知キーは無視、欠損は既定値、**自分より新しい版のファイルは拒否**する。読めない値（NaN / Inf）は書かず、その節を保存しなかったことを画面で知らせる。
- **地図の航空写真レイヤ**: 背景を淡色地図／航空写真（国土地理院 `seamlessphoto`）の 2 択に。市街地・杉林・水面などを目で確かめて、植生高や環境区分の当てずっぽうを減らすための機能で、**写真から入力へ値を流す導線は作らない**。

### 変更

- **実行ボタンの表記と配置を全フローで統一**（「実行」＋進捗バーの右）。**強調（青）は「走らせる」ボタンだけ**に使う。
- **ランチャーのボタンを意味で分類**: 「走らせる」1 個と「別の窓を開く」4 個をボタンに残し、「ファイル／OS へ出る」操作はメニューバーの「ファイル」へ移した。実行完了ダイアログに「保存先を開く」を用意し、結果フォルダが要る瞬間はその場で開けるようにした。
- **条件探索の窓ヘッダを一括シミュレーションと同じ情報設計に**（ラベル付きの枠＋読み取り専用＋🔒）。凍結方式はアプリ全体の原則なのに、見た目が窓ごとに違って原則を裏切っていたため。
- **スイープに「振らない側の条件」を表示**: 比較タブと同じ器（項目名＋ベース値）に軸のラジオを載せ、開始・終了・点数は選んだ軸の行に現れる形へ。従来は周波数やアンテナ高が今いくつなのか画面に無く、確認のためにタブを往復する必要があった。
- **グラフウィンドウを Tk 化**: `Toplevel` ＋ 埋め込みキャンバス＋ ttk の操作系へ組み替え、matplotlib はプロット領域だけを担当する。**縮小したときに文字や凡例が重なる問題が構造的に解消**し、テーマ・DPI 追従・見切れ検査の対象にも入った。図はライト固定（印刷される成果物のプレビューとして見せるため）。ウィンドウタイトルからソフト名を外し、上部の余白も詰めた。
- **グラフウィンドウは唯一インスタンス**: 実行のたびに開き直す（結果を並べて比べるのは条件探索の担当）。
- **DEM が連続して取れないときに打ち切る**: 経路の全点が失敗し続けるとき、最後まで走らずに中止してプロキシ設定を確認するよう促す。実測では、接続できない環境で 151.6 秒かけて全点 0m を返していたところが 12.2 秒で止まるようになった。**海上・域外（404）は通信失敗と区別する**。

### 修正

- **配布版で地形断面の図がまったく作られなかった問題を修正**（`2.6` のプレリリース版でのみ発生。`2.5` 以前は影響を受けない）。実行ファイルに同梱されていない標準ライブラリを描画処理が読み込もうとして失敗していた。症状は 2 つの形で出ていた——単一の実行では「グラフを準備中…」の表示のまま戻らず（ウィンドウ自体は操作できる）、一括シミュレーションでは**エラー 0 件で完走するのに**経路ごとの断面 PNG・詳細レポート・KML だけができず、サマリだけが残った。実行ファイルの作り方（同梱から外すモジュールの指定）が原因で、ソースから動かす場合には起きない。
- **画面の操作中に予期しないエラーが起きたとき、何も知らせずに固まったように見える問題を修正**。これまでは記録も表示もされないまま処理だけが止まっていた。今後はログに詳細（発生箇所を含む）を残し、ログの保存場所を書いたダイアログでお知らせする。
- **座標を度分秒（DMS）表記にしたとき、一部の座標が壊れた文字列になり読み戻せなかった問題を修正**。`34.8` や `132.6` のような値で秒が `-0.0` と表示され、その行は座標として読めなくなっていた（実測で 0.1 度刻みの約 4 割）。DMS 表記を使う場合、バッチの行が実行できない・プロジェクトファイルにバッチの行が保存されない・プロジェクトを読み直すとバッチ画面が開けない、という形で表面化した。
- **標高が 1 点も取れなかった地形をキャッシュしない**: 一時的な通信失敗で全点 0m になった地形が残り、以後その経路が「標高 0m の平野」として計算され続けることがあった。
- **画面が狭いときの逃げ道**: 各ウィンドウに共通のスクロール受け皿を入れ、拡大率の高い環境や小さい画面でも下端のボタンへ届くようにした。あわせて**画面サイズの変化（リモートデスクトップの解像度変更など）でも寸法を測り直す**ようにし、見切れ検査から「画面に入りきらないときは免除する」条項を外して 96/120/144 dpi の全ケースを検査対象にした。
- **日本語 UI の太字が中国語・韓国語の字形になる問題**: 日本語表示の本文書体を Yu Gothic UI に切り替えた（太字にした瞬間に別言語のフォントへ落ちていた）。
- **地図の出典表記が表示されていなかった問題**を修正（国土地理院の利用規約で求められる表記）。
- 🔴 **プロジェクトファイルの上書き保存に失敗すると、前のプロジェクトが壊れて残る問題を修正**。保存先を直接開いて書いていたため、**開いた時点で中身が消え**、途中で失敗すると読めないファイルだけが残っていた（実測では 56KB のプロジェクトが 42KB の壊れた状態になった）。一時ファイルへ書き切ってから置き換える方式に改め、**保存に失敗しても前のファイルはそのまま**残るようにした。ディスクの空き不足など、どの失敗でも同じように守られる。
- 🔴 **壊れたプロジェクトファイルが「読めてしまう」問題を修正**。一部が壊れている場合に、その部分を**無かったことにして読み込みが成功**していた（条件が壊れていれば条件ごと、地点が壊れていれば地点ごと）。**そのまま上書き保存すると、壊れていた部分の中身が消えます**。**アプリが書かない形（`null` や、想定と違う型）は壊れているものとして扱い**、「ファイルが壊れています」と伝えるようにした。⚠️ **項目が丸ごと無い場合は従来どおり既定値で読めます**（古い版で保存したファイル・一部だけを手で書いたファイルは開けます）。空欄を意味する箇所（中継の区間ごとの周波数・利得＝共通設定を引き継ぐ）は、これまでどおり空のまま読めます。あわせて、一部の壊れ方で英語の内部メッセージが出ていたのも直した。
- **バックグラウンドの処理（一括・条件探索・中継経路）が失敗したとき、ログに発生箇所が残らなかった問題を修正**。これまではエラーの一行だけが記録され、どこで起きたのか追えなかった。**経路 1 本ごとの失敗**（計算・保存・レポート描画）も同じく記録されるようにした。

### ドキュメント

- **README の「UI 設定」節を「メニュー」節に改め、メニューバーの全項目を載せた**。従来は 12 項目のうち 6 項目しか載っておらず、**ファイルメニュー（プロジェクトを開く／保存、パラメータ読込、結果フォルダを開く）が丸ごと抜けていた**——プロジェクトファイルの唯一の入口が、メニューの一覧に 1 行も無い状態だった。設定 > 座標形式とヘルプ > バージョン情報も欠けていた。
- あわせて**節名と中身の食い違いを解消**した。「UI 設定」節には設定でないもの（マップウィンドウの説明）が同居していたので、**マップウィンドウを独立した節へ移した**（目次からも辿れる）。
- **座標形式（DD / DMS）の説明を追加**。表記（表示）の設定であって入力モードではなく、**入力欄はどちらの書き方でも受け付ける**こと、欄の整形は表記の切り替えや読み込みのときに走り**入力の確定では走らない**ことを明記した（実機で「入力しても表示が変わらない」を不具合と受け取られたため）。
- グラフ画面の節にあった**古い記述を削除**した（実在しないメニュー名で設定の読み込みを説明していた）。プロジェクトファイルの詳細は独立した節のままにし、メニューの表からリンクする形にしている。
- **再発防止**: メニューバーの全項目が README の「メニュー」節に載っていることを検査するテストを追加（`tests/test_docs_consistency.py`）。対象の一覧はテストに書かず**実装から取る**ので、メニュー項目を足せば README を直すまで検査が通らない。
- **主な機能の一覧をカテゴリ分けした**（20 項目のフラットな箇条書きだった）。**実行フロー（個別 / 一括 / 条件探索 / 中継経路）／地図／計算モデル／出力・レポート／入力の再利用／画面**の 6 群に整理し、実行フローは「何を調べたいか」で選べる表にした。あわせて開発者向け README の一覧が 2 箇所古かったのを直した（マップウィンドウの連続追加モードと、条件探索が載っていなかった）。
- **バイナリ版の動作環境から「Python: 不要」の行を外した**（要件の表に「不要」と書くのは自己矛盾）。内容は表の下の注記に移し、「Python のインストールは不要です」と 1 行で答える形にした。
- **公開されるドキュメントから、非公開の課題管理番号への参照を削除した**。課題台帳は公開リポジトリに含まれないため、番号だけ書かれても読者には辿れない。番号を落として理由を地の文で書く形に改めた。**同じ混入を機械的に止める検査を追加**（追跡下の Markdown 全体が対象。対象ファイルの一覧は持たず git に問い合わせる）。

### 開発環境

- **テストを宣言した環境でしか回せないようにした**。ビルドは以前から「検証にもビルドにも使う唯一の環境」を環境変数で宣言させ、未設定なら止まる作りだったが、**テスト側には何の強制も無かった**——別の環境で検証して別の環境で実行ファイルを作れる状態だった。実際に、別の環境で回すと依存の版がずれて 4 件が失敗し、宣言した環境では全件成功する（＝どちらの結果を見ているのか実行者に分からない）。宣言と食い違う環境で pytest を起動すると、理由と直し方を示して収集前に止める。**環境変数が未設定なら何もしない**ので、CI や新しい clone には影響しない。
- **バックグラウンド処理の失敗を traceback つきで記録することをテストで縛った**（実行して確かめる＋3 モジュールをソースで確認）。
- **配布ビルドに「同梱漏れ」の検査を追加**（ビルドを中止する）。同梱物が無条件に読み込むモジュールが実行ファイルから除外されていたら、その場でビルドを止める。ソースから動かすテストでは原理的に検出できない種類の欠陥で、実際にプレリリースを 1 度出荷してしまった。検査に必要な情報はビルド時のレポートに元から出力されていたが、読む仕組みが無かった。
- 実行ファイルから標準ライブラリを除外する設定を全廃した（削減量は数百 KB で、依存ライブラリの内部が変わるたびに壊れ得る作りだった）。
- 配布ビルドを**テストと同じ仮想環境**で行うようにした（従来は PATH 上の Python でビルドしており、推移的依存が食い違ったまま出荷され得た）。環境は環境変数で明示する宣言制とし、未設定なら中止する。
- 依存ライブラリの固定版を更新。開発用の依存を `requirements-dev.txt` として分離した（新規環境でテスト手順が再現できなかったため）。

---

## [2.5] — 2026-07-30

テーマ「同一経路の条件探索」。**1 本の確定した経路を、条件を変えて掘る**ための第 4 の実行フロー（条件探索）を追加し、その前段として表示・出力層の要望をまとめて片付けた一巡。計算パイプラインが 2 相（DEM 取得＝座標とサンプル数にしか依存しない／リンクバジェット＝純関数）であることを土台に、**DEM 取得 1 回＋純計算 N 回**で条件を掘れるようにしている。

### 追加
- **条件探索ウィンドウ（比較 / スイープ）**: ランチャーから開く独立した画面。経路（座標）とサンプル数はランチャーの値に固定し、**地形の取得は 1 回だけ**で N 通りの条件を計算する。
  - **比較**: ベース（ランチャーの現在値・読み取り専用）に対し、**比較条件を最大 5 列**まで並べる。周波数・送信電力・送受信アンテナ利得・受信感度・送受信アンテナ高・植生高・降雨強度・環境・回折モデルを条件ごとに変えられる（座標とサンプル数は変えない＝経路そのものの比較は一括シミュレーションの担当）。
  - **スイープ**: 1 軸（アンテナ高・周波数・降雨強度など）を 2〜41 点で振り、判定が反転する位置を見る。
  - **↻ ランチャーから更新**: ランチャー側の変更（座標を含む）を明示的に取り込む。押すまでは画面に出ている値で計算する（表示と計算が食い違わない）。編集済みの条件欄は巻き戻さない。
- **条件探索レポート（A4 縦 1 枚）＋ CSV**: `results/scenario_YYYYMMDD_HHMMSS/` に `scenario.html` / `scenario.csv` を保存。
  - 比較＝ベースとの差分表。**Δ はセル内に併記**（列数を倍にしない）、**変えた入力のセルだけ**色を付ける（どの列のどの欄を変えたかを目で拾えるように）。
  - スイープ＝**折れ線＋数値表**。判定のしきい値（マージン 0 dB）を引き、点は判定で塗り分ける。マージンの振れ幅が大きいときは縦軸を symlog に切り替えてしきい値付近が潰れないようにする。**最大 41 点でも 1 列のまま A4 1 枚**に収まる（行を詰め、図の高さを配分する）。
- **一括レポート `report_all.html`**: バッチのサマリと全経路のレポートを 1 文書へ連結。**Ctrl+P 一発で全ページぶんの PDF** になる（PDF エンジンは従来どおり非導入）。台帳のサムネイルから文書内の該当経路へ飛べる。既存の `summary.html` / `{id}/report.html` は単体共有用にそのまま残る。バッチ完了ダイアログから「サマリを開く / 全ページを開く」を選べる。
- **バッチ表に水平距離列**: 行の座標から計算した水平距離を読み取り専用で表示。座標の打ち間違いは距離が極端な値として現れるため、実行前に気づける。

### 変更
- **距離表示を m へ統一**（内部計算は km 据え置き）: 人が読む面（グラフ軸・パネル・レポート・地図バッジ）はすべて m 表記に統一し、書式を `units.py`（純関数）へ集約した。**出力契約の互換破壊**: `summary.csv` の `slant_km` → `slant_m`、`terrain_profile.csv` の `Distance_km` → `Distance_m`（`Elevation_m` との非対称を解消）。
- **座標形式メニューを日本語表記に**: `十進度（DD）` / `度分秒（DMS）`（en は `Decimal Degrees (DD)` / `Degrees Minutes Seconds (DMS)`）。
- **個別シミュレーションのグラフから回折モデル切替を撤去**: グラフ上のトグルは実行中のパラメータをその場で書き換えるため「ランチャーで Single を選んだのに保存レポートは Deygout」という齟齬を作れた。回折モデルの入力口はランチャー 1 か所に統一し、モデルの比較は条件探索が担当する。読み取り専用の表示は残す。
- **補助テキストをテーマ色へ**: 固定色 `gray` の直書きをやめ、テーマから引いた色に統一（ライト 3.78→4.41:1、ダーク 4.32→6.60:1 とコントラストが改善）。
- **F1 遮蔽率の表示を 100% で頭打ちに**: 深い山越えでは「F1 半径に対する侵入深さ」が 7000% 相当になり、「率」として誤読される。表示（グラフパネル・`report.txt`・台帳・`summary.csv`）は 100% で打ち切る（計算内部は従来どおり生値を使う）。
- **UI フォントをアプリ全体で統一**: ウィジェットごとの書体ベタ書き（`Arial` 8/9pt）をやめ、**sv_ttk のテーマが持つ本文フォント（Segoe UI Variable）を単一の出所**にした。ランチャー・バッチだけ Arial、条件探索・グラフ・地図は既定フォント、しかもテーマは入力欄にだけ別フォントを当てる——という三重のばらつきを解消し、Windows の UI フォント・テーマ・DPI に追従する。
- **条件探索の画面を実機フィードバックで手当て**: 項目名・スイープ軸に**単位を表示**（レポートと同じ単位表を使うので画面と出力で表記がずれない）、入力欄の**幅を列で統一**（比較タブ・スイープタブとも、数値欄と選択欄の右端が揃う）、結果一覧に**余白**（行間・左右）を設定。
- **モジュール分割**: `report.py`（1018 行）を **`report_common.py`（A4 骨格・ヘッダ/フッタ・文書外枠）/ `report_path.py`（経路ごと）/ `report_summary.py`（サマリ・連結）** の 3 つへ分割。レポートは「シート断片を返す関数」と「文書に包む関数」に分け、条件探索の新テンプレは断片を 1 つ足すだけで載るようにした。

### 修正
- **フル HD の画面でランチャー下段のボタンが見切れた問題**: 1920×1080・100% 表示の PC で、最下段の「マップウィンドウ」「条件探索」がラベルの読めない数 px の帯にまで潰れ、**その 2 つの機能へ到達できなかった**。ランチャーが必要とする高さ 1023px に対し、画面から使える高さが 990px しかなく、33px が下端から削られていたため（下端に近いボタン列から潰れる）。**ロゴの表示高に上限を設けて必要高を 973px に収めた**（ロゴは縦横比を保ったまま縮む。入力欄・ボタンの余白は従来どおり）。
  あわせて**見切れ検査の免除条項を塞いだ**: 従来のゲートは「画面に入りきらないときは免除する」と書かれており、しかも開発機の画面が実機より縦に長いため、**実機で溢れていても緑のまま**だった。出荷先の画面サイズ（フル HD・100%）を定数として与え、全ウィンドウの必要寸法がそこに収まることを検査する。
- **条件探索に案件情報を表示し、凍結方式へ統一**: 案件名・メモはレポートに刻印されるのに条件探索の画面には出ておらず、しかも**実行の瞬間にランチャーを読み直していた**ため、窓を開いたあとにランチャー側を変えると画面に無い値が成果物に載り得た。開いた時点でスナップショットして表示し、↻ で明示的に取り込み、実行は表示中の値で行う（座標・パラメータと同じ扱い）。**ランチャーから分岐する窓は今後もこの方式で統一する**。
- **条件探索の入力検証**: 比較・スイープとも値域を検証していなかった（周波数 0 や負の値で実行ごと失敗し、負の高さ・負の降雨は黙って計算が通り、`inf` は判定 OK まで出た）。単一実行と**同じ値域の表**を使って実行前に弾くようにし、NaN / Inf も拒否する。エラーメッセージは項目名つきで日本語・英語に対応（従来は Python の生メッセージが出ていた）。
- **バッチの共通設定の入力検証**: バッチ画面の共通設定（周波数・電力・利得・感度・植生高・k 係数・サンプル数・降雨強度・環境・回折モデル）は読み取り専用表示だが、値はランチャーの入力をそのまま写したもので、**単一実行を一度も押さなければ無検証のまま実行に使われていた**（周波数 0 で地形取得後に失敗、サンプル数 999999 で固まる、負の値や `inf` は黙って通る）。条件探索と同じく**単一実行と同じ値域の表**で実行前に弾くようにした。あわせて、バッチ行の値域が別ファイルに数値で二重定義されていたのを同じ表の参照へ寄せた。
- **DPI 追従**: 拡大率の違うモニタへ移す（または高 DPI モニタで起動する）と、**窓の大きさだけが変わって文字は小さいまま**だった。sv_ttk のフォントはピクセル指定で `tk scaling` の影響を受けず、Tk 8.6 自体も DPI 変更時にフォントへ何もしないため。96dpi 基準のピクセル数を保持して実 DPI 倍で当てるようにし、モニタ移動を検知して貼り直す。文字が大きくなれば必要な寸法も増えるので、**全ウィンドウの測り直しまでを対にした**。
- **バッチ CSV のヘッダ大小混在で読み込めなかった問題**: `ID,Start,End,…` のように大文字混じり（内容は正しい）ヘッダの CSV は、必須列ありと判定した直後に「Row 2: 'id' is empty」という矛盾したエラーで落ちていた。ヘッダ判定だけ正規化して行の読み出しは元のキーで引いていたため。列名は前後空白と大小を無視して読むように統一した（アプリが書き出した CSV は小文字ヘッダなので、往復していた利用者には影響しない）。
- **同一秒に始めた実行が前の結果を上書きし得た問題**: 一括シミュレーションと条件探索の出力フォルダは秒精度の時刻で命名しており、同じ秒に 2 回始めると同じフォルダを共有して `summary.csv` などが黙って上書きされた。衝突時は連番（`batch_…_2`）を付けて必ず別フォルダにする。
- **大小だけが違う経路 ID で結果が混ざり得た問題**: `p01` と `P01` は別 ID として通るが、Windows のファイルシステムは大小を区別しないため成果物が同じフォルダへ書かれていた。実行前の重複チェックで大小を区別せず弾くようにした。
- **CSV を表計算で開いたときに値が数式として解釈され得た問題**: `summary.csv` の備考・エラー欄は自由文字列で、`=` `+` `@` などで始まると Excel 等が数式として扱う。文字列として明示するようにした（負の数値はそのまま数値として読める）。同じ規則を条件探索の `scenario.csv` にも適用している。
- バッチ表の水平距離列を追加した際、ウィンドウ幅が中身に足りず右端が見切れる問題を修正。
- **ウィンドウ寸法の決め方を 1 つに集約（`views/window_fit.py`）**: 「窓を中身に合わせる」実装を全窓で共有し、地図ウィンドウ（唯一まだ固定寸法だった）も実測追従にした。バッチは列幅同期のたびに測り直すので、CSV インポートで列が広がっても見切れない。画面端に残す余白も窓ごとにばらけていたのを統一。
- **条件探索のウィンドウを中身に追従**: 比較条件を上限（5 列）まで足すと右端の列が窓外へ出て見切れていた。列の増減のたびに測り直す（既定幅は下限として扱い、縮めはしない）。
- **ランチャーのウィンドウ幅を中身に追従**: 固定サイズ窓なのに幅だけリテラル（450px）で持っており、フォントや翻訳で必要幅が増えると右端の入力欄が黙って詰まる。高さは 2.4 で同じ理由から実測追従にしてあったので、幅も同じ仕組みに揃えた。

### 開発環境
- **回帰コーパス（物理モデルのゴールデン固定）**: 代表回線 26 本の入力一式・**実 DEM 由来の標高配列**・`LinkBudgetResult` 全項目を `tests/data/golden_links.json` へ凍結し、標高配列から再計算して突き合わせる（ネットワーク非依存）。生成器は `tests/golden_corpus_gen.py`（手動実行）。モデルを意図して変えたときは再生成し、JSON の差分＝影響範囲をレビューしてからコミットする。
  - 初版は「かすめ（grazing）」帯を持たず、回折・K 推定の係数を変えても全通過した（深く塞がれた回線は係数が飽和して効かない）。回折損が数 dB の回線を追加し、その帯を持つことを網羅性テストで固定した。
- **純関数としての不変条件をテストで固定**: 再実行がビット一致／terrain を使い回しても毎回作り直しても同値／terrain・パラメータを書き換えない／評価順に依存しない。条件探索が成り立つ前提そのもの。
- **カバレッジ計測の死角を修復**: `report.py` 分割時に計測対象リストが古いまま出力層がまるごと計測外になっていた。対象の網羅をテストでゲート化（CI の pyright 対象と同じ方式）。
- **見切れの横断ゲート（`tests/test_window_fit.py`）**: 全ウィンドウを 1 つのゲートで検査する。①開いた時点で中身が収まる（日本語・英語の両方）②中身が増える操作のあとも収まる③**`views/` の Toplevel 生成箇所を静的に洗い出し、ゲートに登録も除外もされていない窓があれば落ちる**。見切れは 5 回再発しており、原因は個々の実装ではなく「新しい窓・新しい増え方が自動で検査対象にならない」ことだったため、そこを塞いだ。
- **テストが人の操作を待たないことをゲート化**: GUI テストからモーダルダイアログ（`wait_window`）・`os.startfile`・ファイル選択・`plt.show` が出ると、人がボタンを押すまでテストが止まる。外部ネットワーク遮断と同じ形で **autouse フィクスチャが既定で塞ぐ**ようにし、呼び出しは記録して検証できるようにした（遮断が外れたこと自体を検出するテストつき）。
- コントラスト比の数値検証を補助テキストへ拡大、距離書式が描画層へ戻らないことの静的ガード、条件探索の各種ガード（取得と地形構築が 1 回・計算が N 回、進捗の相が 100% まで届く、シート CSS のスコープ、軸に並ぶ量がすべて結果を動かすこと）を追加。

### 互換性
- **`summary.csv` の `slant_km` 列は `slant_m` に、`terrain_profile.csv` の `Distance_km` は `Distance_m` に変わる**（値も m）。旧列名を前提にした集計シートがある場合は読み替えが必要。
- バッチ CSV の入力スキーマは変更なし。既存の設定ファイル・DEM キャッシュ・結果フォルダはそのまま使える。

---

## [2.4] — 2026-07-25

テーマ「A-0（アンテナ初期指向 AZ/EL）＋出力層 v2（A4 ドロップイン）」。スクリーニング結果を **そのまま報告書へ綴じ込める A4 成果物** にし、経路に **初期指向 AZ/EL** を載せる一巡。レポートを HTML＋print CSS で「無加工で他の A4 成果物へ綴じ込める確定 1 枚」に作り込み、PDF 化はブラウザの Ctrl+P に委任（PDF エンジンは非導入）。

### 追加
- **レポート v2 ＝ A4 ドロップイン**: per-path（`report.html`）／summary（`summary.html`）を **A4 縦の印刷確定枠**（`@page A4`・`@media print`）で生成。画面では A4 用紙（`.sheet`）を描いて WYSIWYG に、印刷では Ctrl+P で **ゼロ依存 PDF 化**（PDF 生成エンジンを内製・依存追加しない＝哲学③⑤）。各ページに **自己同定ヘッダ/フッタ**（タイトル・案件名・生成日時・ID・版）を付す。印刷ダイアログでは「ヘッダーとフッターをオフ」を前提とする（ブラウザ挿入の印刷ヘッダは CSS 抑制不可のため自前ヘッダで代替）。
  - **per-path を A4 縦 1 枚に収める**: 本文を JS の「縮小フィット」で実測し、印字域を超える分だけ一様縮小して 1 頁に固定（重複・低価値行の削除、地図アスペクト 15:5、地形断面のデータ表を削除）。
- **案件情報（案件名・自由メモ）**: レポートの自己同定ヘッダに載る任意メタ情報を **ランチャーに入力欄を新設**（source of truth）。**シングル（保存時）もバッチも同じ値を踏襲**する。バッチでは Common Settings と同じく 🔒 読み取り専用スナップショットとして表示し、「↻ ランチャーから更新」で取り込む。案件名＝per-path/summary 両ヘッダ、自由メモ＝summary のヘッダ直下（単一レポートにも表示）。タイトル文字列そのものの自由編集は行わない（テンプレエディタ化しない・案件名フィールドで代替）。
- **アンテナ初期指向 AZ/EL（A-0）**: per-path レポートの Site Info に、相手局へ正対するための **真方位 AZ ＋仰角 EL** を両端ぶん表示。既存データ（座標・アンテナ高・地形標高・地球曲率）から幾何計算する純関数（`models.bearing_deg` / `models.elevation_angle_deg`）で、新規入力はゼロ。両端で別値（AZ は大圏逆方位、EL は高低差項の符号反転）。「初期指向。最終調整は現場の RSSI 最大化で」と注記する。**AZ が真方位である旨と磁気コンパス使用時の偏角補正（日本＝西偏約 7〜9°）は、レポートではなく同梱 README（`README_binary_ja/en.md`）に記載**（A4 1 枚に収めるためレポート本文の注記は最小限に留める方針）。
- **全経路を 1 枚に俯瞰する地図（summary）**: `report_map.render_paths_map`（north-up・回転なし）で、全端点の外接矩形を出力アスペクトへフィットさせた俯瞰地図を `summary.html` の 1 ページ目に埋め込む。経路は判定ステータスで色分け（OK 緑 / NG 橙赤 / ERROR 濃赤・台帳の行色と単一ソース `map_graphics.STATUS_RGB`）、中点に path_id ラベルを付す（本数が多いときはラベルを省く）。タイル取得に失敗したら地図を省いて注記（ベストエフォート）。

### 変更
- **進捗トランスポートを 1 部品へ統合**: 単一実行とバッチが各々で持っていた「キュー＋`after` ポーリング」を **`views/progress.py` の `ProgressPump`** に括り出した。**狙いは重複の削減ではなくライフサイクルの統一**＝従来バッチのポーラは生成時に 1 回起動して永久に回り、単一のポーラは実行ごとに起動・停止するという非対称があり、これ自体が後述の停止バグを生んだ。両者とも「実行のあいだだけ回す」に揃え、同種のバグが構造的に成立しないようにした。2 者の差は 2 つのパラメータに閉じている（単一＝最新 1 件だけ描画／バッチ＝`done`・`complete` が結果を運ぶので全件を順序どおり処理）。あわせて**実行中にウィンドウを閉じたとき破棄済みウィジェットへ `after` し続ける経路**も塞いだ。なお単一とバッチのパイプライン本体は統合しない（出口が対話グラフと確定成果物で異なる＝2.3 で確定した設計思想）。
- **レポート v2 の仕上げ（β 検証で調整）**: 実出力を見ながら詰めた分。
  - **リンクバジェット表から総損失行を削除**（上部の KPI カードには残す＝同じ値を 2 箇所で持たない）。
  - **数値の桁揃え**: 値と単位を分けて右寄せ＋`tabular-nums` で桁位置を固定し、縦に読めるようにした。
  - **AZ/EL のラベルを短縮**（`初期指向 TX→RX` → `TX指向 (AZ/EL)`／RX 側も同様）。A4 1 枚の枠に収めるため。
  - **ヘッダーを刷新**: 左は `案件名 - タイトル` の 1 行（バッチ per-path は末尾に path_id）、右は生成日時のみ。単一レポートで save_dir のタイムスタンプが ID として露出していた不具合も解消。版はフッタで自己同定するのでヘッダからは外した。
  - **`<title>` を案件名ベースに**: ブラウザのタブ名と、Ctrl+P で PDF 保存するときの既定ファイル名になるため。
  - **フッタの種別表記を分離**: 単一＝「個別シミュレーション」／summary＝「一括シミュレーション」。
  - **地形断面グラフの視認性**: 背景を灰色から白へ、軸ラベル・タイトルのフォントを拡大（印刷・縮小表示でも読めるように）。
- **summary 台帳の可読性を刷新**: `table-layout:auto` で各列を内容幅に合わせ（ヘッダを語中で折らない・長い数値がはみ出さない）、ヘッダの単位は 2 行目へ、列を縦罫線で分離、フッタを用紙下端へ。極端値を含む台帳でも A4 印字域に収める。
- **`path_id`・備考に文字数上限**: 長すぎる値は台帳の列幅を押し広げ A4 レイアウトを崩すため、実行前の `validate_rows` で弾く（手入力・CSV 取込の共通チョークポイント）。上限は path_id 16 文字・備考 40 文字。
- **`infrastructure.py` を分割**: `config.py`（アプリ設定）＋ `dem.py`（DEM/タイル取得・キャッシュ）へ分離（DEM 系を触る変更のためのモジュール整理）。

### 修正
- **設定・キャッシュ・結果・ログの保存先が起動方法によって変わる**: 設定ファイル・結果フォルダ・ログ・DEM キャッシュの場所が**カレントディレクトリ基準の相対パス**だったため、ショートカットの作業フォルダやコマンドラインの cwd 次第で**保存先が黙って移っていた**。エラーも警告も出ず、設定が既定値で開き、DEM を取り直し、過去の結果が「結果フォルダを開く」からも見えなくなる（作業フォルダが exe の隣を指す通常のショートカット起動では起きないため、長く残っていた潜在的な不具合）。書き込み先の基準を **exe（スクリプト実行時はソース）の位置**へ固定した。**通常の起動では従来とまったく同じ場所を指す**ので、既存の設定・キャッシュ・結果はそのまま引き継がれる。回帰ガード＝`tests/test_paths.py`（cwd を変えても保存先が動かないこと・従来と同じ場所を指すこと・単一/バッチ/地図の全経路が同じ解決を通ること）。
- **DEM 取得の一時失敗がプロセス終了まで残り標高が誤る**: DEM タイル取得に失敗したタイルを記憶する負キャッシュ（`_failed_tiles`）が、**恒久欠落（日本域外・海上で GSI が返す 404）と一時失敗（タイムアウト・接続断・5xx）を区別せず**両方を永久保持していた。このため通信の一時的な失敗が同一プロセス中そのタイルを汚染し続け、回復後も取得済みタイルを無視して標高が粗レイヤ由来か 0.0 に化けたまま**黙って誤った計算結果**を出しうる（常駐する GUI で顕在化。単一・バッチ・プレビューは同一の標高取得経路を共有するため全フローに波及）。負キャッシュへの登録責務を `get_elevation` から `_fetch_tile` へ移し、**HTTP ステータスで恒久/一時を分離**（404 のときだけ登録・それ以外は登録しない）。戻り値契約は不変のためプリフェッチ／淡色地図／レポート地図は無改修。回帰ガード＝`tests/test_dem.py::TestFailedTileNegativeCache`（一時失敗→回復で標高を取得し直すこと・404 は記憶して再取得を抑止することを検証）。
- **ダークテーマでツールチップの文字が読めない**: 入力欄の説明ツールチップが、背景は OS 固定色・前景はテーマ追従という**別々の出所**から色を取っていたため、ダークでは白文字が淡いグレーの背景に載って判読不能だった（コントラスト 1.06:1）。テーマを一度切り替えると発生する。配色を `views/theme.py` から前景・背景まとめて取るようにした。
- **メニューの配色が実際には適用されていなかった**: 症状は「ライトテーマでアクティブ行のカスケード ▶ が背景と同化して見えない」。原因は色の取得元で、`ttk.Style().lookup("TFrame", "background")` は sv_ttk（Sun Valley）では**常に空文字を返す**（このテーマは背景色・前景色を設定せず外観を画像で描くため）。空でも例外にならないので、「テーマ色を明示適用する」コードが**黙って何もせず終了**していた。この取得方法を使っていた 4 箇所すべてが同じ状態で、**ダークモードのメニュー ✓（先に修正したはずのもの）とバッチ表のキャンバス背景も、実際には効いていなかった**。配色の出所を `views/theme.py` へ単一化し、sv_ttk 自身の色定義から読むようにしたうえで、バッチの行右クリックメニュー（従来テーマ適用の対象外）も含めて載せ替えた。アクティブ時も前景色は変えない方針にしている（`tk.Menu` の ✓ の色は状態別に指定できないため、アクティブだけ別色にすると ✓ か ▶ のどちらかが必ず溶ける）。回帰ガード＝`tests/test_theme.py`＝色が付くことに加え、**前景と背景のコントラスト比が 4.5:1 以上あることを数値で検証**する。
- **単一実行の進捗表示が実態とずれていた**: 標高取得が速くなった結果（下記のバッチ進捗の改修）、バーが担当する区間は暖機時 0.035 秒になった一方、その後の matplotlib 遅延 import とグラフ構築（計 約 0.6 秒・メインスレッドを占有）は**バーの管轄外**で、しかもその直前にラベルを「準備完了」へ戻していた。実際には固まっている間ずっと「準備完了」と表示していたことになる。グラフ描画に入る前に **「グラフを準備中…」** を表示し、グラフが画面に出る直前に待機状態へ戻すようにした。対話グラフは pyplot/TkAgg でメインスレッド必須のためワーカーへは出せず、偽の進捗も出さない（バッチの段階ラベルと同じ方針）。
- **ランチャーの「マップウィンドウ」ボタンが表示されない**: ウィンドウ高さが `450x900` のリテラル固定だったため、2.4 で「案件情報」グループ（案件名・メモ）を足して必要高さが 931px になった時点で最下段のボタンが可視領域外へはみ出していた。ウィンドウは固定サイズなのでユーザーは広げて回避することもできない。高さのリテラルを廃し、**中身の必要量（`winfo_reqheight`）から決める**方式へ変更（下限 900px・上限は画面高さの 92%）。以後、入力グループを足しても追随する。
- **バッチのプログレスバーが実態を表さず、途中で GUI ごと固まる**: 独立した 3 つの欠陥が重なっていた。①バーの駆動源が DEM 標高取得のみで、体感時間の大半（レポート描画）がバーの管轄外だった → 配分を「取得 15% / 描画 85%」とし、描画中は偽の進捗を出さず段階ラベルで示す。②成果物生成（per-path PNG/HTML/KML・サマリ地図/HTML/KML）を GUI コールバックで呼んでおり、その間メインスレッドが停止していた → `batch` のワーカースレッドへ移動（「matplotlib はメインスレッド必須」は `save_profile_png` が pyplot から `FigureCanvasAgg` へ移行した時点で**失効していた制約**）。③単一実行が進捗コールバックに `root.after` を直接渡しており、`fetch_elevations` がグローバルロック保持中に 1 サンプルごと呼ぶため Tcl 呼び出しが全ワーカーを直列化していた（同一条件で単一 ≒1.0 秒／バッチ ≒0.035 秒）→ バッチと同じキュー＋50ms ポーリング方式へ統一。
- **単一実行の進捗ラベルが完了後も残る（進捗のキュー化で作り込んだ回帰）**: 進捗をキュー化した際、停止フラグを下ろしても `after(50)` 済みのポーリングが 1 回残り、積み残しの進捗が完了表示を上書きしていた。キュー破棄と `_poll_progress` の早期 return の二重防御で解消（ワーカーは完了通知の後にも進捗を push しうるため、キュー破棄だけでは不足）。

- **バッチが地形キャッシュを素通りしていた**: 単一実行は `fetch_elevations_cached`（TX/RX 座標＋サンプル数が同じなら DEM を再取得しない）を使うのに、バッチだけが素の `fetch_elevations` を呼んでおり、同一バッチを再実行するたびに DEM を取り直していた。バッチも単一と同じキャッシュ付き取得へ統一（1 行だけ直しての再実行でも、変えていない行の取得が消える）。実行前のタイル一括取得（プリフェッチ相）はバッチには設けない＝遅延取得でも総取得量は変わらず、得られるのは進捗の見え方だけのため。

### 開発環境
- レポート出力の回帰ガードを拡充（A4 骨格・案件情報・AZ/EL・全パス地図・台帳可読性・文字数上限）。
- **ランチャー→バッチのメタ伝播スモーク**（`tests/test_smoke.py::test_report_meta_flows_from_launcher`・表示依存）を追加＝「ランチャー＝source of truth」の統一配線の回帰ガード。
- **外部ネットワーク遮断ゲート**（`tests/conftest.py`）: socket 層で非 localhost 宛の接続を遮断し、テストが実ネットワークへ出ないことを常時保証（正当な通信は `@pytest.mark.network` でオプトアウト）。契機＝サマリ地図生成をワーカースレッドへ移した結果、ユニットテストが地理院タイルを叩き始めていたのに**オンラインでもオフラインでも緑**のままで検出できなかったこと。
- **実行時制約をコメントからテストへ昇格**: 「〜してはいけない／必須」がテストで守られているかを全件棚卸しし、未強制だったものをガード化した。いずれも制約を壊すと実際に落ちることを確認済み。
  - レポート描画を並列化しない（`mpl_fonts` が `matplotlib.rcParams` をプロセス共有で書き換えるため）＝`test_path_rendering_is_never_parallel`。
  - DEM タイルキャッシュのロック保持中にネットワーク取得を行わない（並列化が無効になる）＝`test_network_fetch_runs_without_holding_the_cache_lock`。
  - マップ実体の破棄経路が `close_map_safely` に集約されている＝`test_map_widget_is_destroyed_only_through_close_map_safely`（静的検査）。
  - スレッド生成規約（`ThreadPoolExecutor` 不使用・`threading.Thread` は `daemon=True`）＝`test_thread_pool_executor_is_not_used` / `test_all_threads_are_daemon`（静的検査）。従来メモリ上の規約でしかなくコードに痕跡が無かった。
- **進捗トランスポートの回帰ガード**: `tests/test_progress.py`＝開始/停止のライフサイクル・停止後に残る 1 回のポーリング・二重 start でポーリング連鎖が 2 本にならないこと・`latest_only` の畳み込み・破棄済みウィジェットでの静かな停止・複数ワーカーからの `push`。scheduler がダックタイプなのでフェイクでヘッドレスに検証できる。加えて `tests/test_smoke.py` に**配線**の確認（単一・バッチとも生成時は止まっている／閉じたら止まる）を実 Tk で追加。
- **進捗まわりを3実行フローで対称化**: 実行フローは 単一 / バッチ / 地図タイル取得 の3つ。①phase 境界ログを3フローに揃えた（従来はバッチのみで、単一のグラフ描画も地図の後処理もログ上見えなかった）。②地図のタイル取得も `ProgressPump` 経由に統一（従来はタイルごとに `after(0, …)` を2回呼ぶ、他フローで廃した書き方だった）。③3フローが同じ部品を使うことを `test_all_execution_flows_use_the_progress_pump` で静的に固定。
- **ランチャーのレイアウト破綻ガード**: `tests/test_smoke.py::test_launcher_window_fits_its_content`＝中身の必要高さがウィンドウに収まることを検証。入力欄を1グループ足すだけで下端が黙って切れる類なので、注意書きではなくゲートで守る。
- **phase 境界ログを追加**: バッチで最も時間を食う区間（per-path のレポート描画・サマリ地図生成）にログ行が 1 つも無く、進捗の不具合を診断する際に所要時間が最後まで測れなかった。バッチ開始・パス別描画・サマリ地図・バッチ全体の所要秒数を `radiosim.log` に記録する。
- **README のテスト件数表記を削除**: 件数は読者に行動可能な情報を与えない一方、テスト追加のたび更新を強制する（実際に実数と 70 件ずれていた）。ガードを足すのではなく主張そのものを撤去してドリフト面を消した。テストファイルの列挙と対象範囲は従来どおり整合ガードが守る。

### 互換性
- レポートは HTML＋print CSS のみで PDF 化する方針を継続（PDF エンジン非導入）。関数シグネチャは単一/バッチ両フローで概ね不変（案件名・メモは既定空で従来表示）。

---

## [2.3] — 2026-07-03

テーマ「座標入力の完成」。バッチのランチャー凍結（Phase D1）と地図からの連続追加（Phase D2・地図をランチャー所有の単一インスタンスに一本化）で、座標入力体験を完成させた。

### 追加
- **バッチ「ランチャー凍結」方式（Phase D1）**: バッチの設計モデルを「シングル＝条件を詰める場／バッチ＝確定条件で成果物を作る場」に転換。ランチャー（シングル）が唯一の真実の源となり、バッチの各行は **行追加時点のランチャー欄を文字列コピーして凍結した確定リンク** になる。
  - **+ 行を追加** は、その時点のランチャー欄（座標・周波数・利得・アンテナ高）を凍結した行を生成する。
  - **共通設定を読み取り専用化**: ランチャーのスナップショットとして表示し、**「↻ ランチャーから更新」** ボタンで現在値を取り込む。
  - **per-row 往復（右クリックメニュー）**: 行ごとに **「→ シングルへ送る」**（座標＋RF をランチャーへロード）／**「⟳ RF をランチャーで更新」**（座標は保持し RF のみ書き戻し）／複製／削除。
  - **アンテナ利得の per-row 化**: `gain_tx` / `gain_rx` を経路ごとに指定可能なリンク識別属性として追加（バッチ表に2列・CSV の省略可列・`summary.csv` / `summary.html` に利得列）。未指定時は共通設定を継承。
- ランチャーに `load_batch_row`（→シングルへ送る の受け口）を追加し、`BatchBuilderWindow` に `config_provider` / `load_params` を注入。
- **連続追加モード（Phase D2）**: マップウィンドウに **「バッチへ連続追加」** モードを追加。地図で TX→RX のペアを置くたびにバッチ行が自動追加され、自動的に次の TX 待ちへリセットされる（+行 追加は不要）。RF（周波数・利得・アンテナ高）は **追加時点のランチャー値で凍結**。バッチ表にある全パスは地図上に TX/RX マーカー＋経路線＋水平距離で表示する。
  - **地図はアプリ唯一のインスタンス（ランチャー所有）に一本化**。「バッチからも地図を開く」第2インスタンスは作らない（ランチャーが本筋・バッチは従属する受け皿）。連続追加モードに入るとランチャーがバッチウィンドウを開く/前面化し、append 先として配線する。バッチを閉じると地図は座標入力モードへ戻る。
  - `MapWindow` のピック宛先を 2 種類の「座標シンク」で表現: 単一書き戻し（ランチャー＝start/end 欄・構築時固定）と連続 append（バッチ＝`append_path`/`existing_paths`・モード移行時に取得）。
  - **バッチの行変更を地図へリアルタイム反映**: 削除・全クリア・CSVインポート・追加・複製、および座標セルの編集確定（FocusOut/Enter）で地図の確定パス表示が即座に追従する（バッチ→ランチャー→地図のコールバック）。並べ替えはパス集合が不変なので再描画しない。一括再構築（インポート/並べ替え）は逐次通知を抑止して終了後に1回だけ更新。
  - **確定パスの端点表示を TX=塗りドット／RX=方位矢じりに変更**（`map_graphics.arrow_icon`）。従来の TX/RX 文字ラベル＋塗り/白抜きノードは、TX/RX が近接・同一座標だと文字もノードも重なって判別不能だった。送受を別形状（ドット↔矢じり）で表し、矢じりは TX→RX 方位を指すので向きも分かる。文字ラベルはアクティブ選択中のパスのみ残す。
  - マウスホイールのスクロール波及を修正（`bind_all` がアプリ全体に効くため、別ウィンドウのホイール操作でバッチ表が一緒にスクロールしていた問題。ポインタが表上のときだけスクロールするよう限定）。

### 変更
- **`batch.py` を分割**: 行数閾値超過（D1 で約1045行）を解消するため、出力生成層（per-path の PNG/HTML/KML・サマリ CSV/HTML/KML、約700行）を新モジュール **`report.py`** へ切り出した。`batch.py` は実行エンジン（CSV I/O・バリデーション・実行）に専念。挙動・公開関数のシグネチャは不変で、純リファクタ（呼び出し元 `views/graph.py`・`views/batch_builder.py`・テストはインポート先の変更のみ）。`report.py` はヘッドレス維持（UI 非依存）。

### 修正
- **バッチウィンドウの UI 不具合 3 件**: `views/batch_builder.py` のみの修正。
  - **ダークモードで共通設定が視認不能**: 共通設定の読み取り専用欄が素の `tk.Entry`＋背景色ハードコードで sv_ttk テーマに追従せず、ダークで薄グレー背景×白文字になり判別不能だった。`ttk.Entry` の readonly に変更しテーマ配色へ追従。
  - **日本語で見切れ・はみ出し**: テーブル見出しの固定文字幅を撤去し、`_sync_header_columns` を「見出し必要幅と行実測幅の大きい方をヘッダ・全行へ適用」する言語非依存の自動調整に刷新（追加/インポートはデバウンス）。下部ボタンの固定幅も撤去し、「↻ ランチャーから更新」を共通設定枠内の専用行へ移して窓外はみ出しを解消。
  - **バッチのプログレスバーが完了後もリセットされない**: 完了・失敗時にバーを 0 へ戻し進捗カウントも消去（シングル実行 `_on_fetch_complete`/`_on_fetch_error` と挙動を整合）。結果サマリは OK/NG/ERR ラベルと `Done: <dir>` ラベルに残す。
- **バッチHTMLレポートの地形グラフが日本語で文字化け（豆腐化）**: 日本語モードで個別グラフを開かずにバッチを実行すると、レポート PNG の軸ラベル・凡例が□で表示される不具合を修正。日本語フォント適用が `views/graph.py` の個別グラフ表示時にしか行われず、その rcParams 設定がプロセスに残ることに依存していたため、操作順によって発生していた。フォント適用を**ヘッドレスな共通ヘルパ `mpl_fonts.py` に単一ソース化**し、バッチのレンダリング（`report.save_profile_png`）でも明示的に適用するようにした（操作順非依存）。`views/graph.py` は同ヘルパへ委譲。

### 開発環境
- **Ruff/Bandit 設定を `pyproject.toml` に明文化**: 従来 CI でフラグ指定していた Ruff の lint 選択（`E9`/`F`）を `[tool.ruff.lint]` へ移し、CI は `ruff check .`（pyproject を単一ソース）に簡素化。`[tool.bandit]` 設定も追加。
- **CI の Pyright 対象に新モジュールを追加**: 2.3 で増えたが登録漏れだった `report.py` / `mpl_fonts.py` / `coords.py` を型検査リストへ追加。
- **GUI スモークテスト `tests/test_smoke.py` を追加**: 全モジュールの import 疎通＋tkinter ルート生成を検証（ヘッドレス CI では `tk.Tk()` を skip）。import で壊れる回帰を機能テストより手前で早期検出する。

### 互換性
- `gain_tx` / `gain_rx` 列のない旧バッチ CSV はそのまま読める（後方互換・利得は共通設定を継承）。環境区分・降雨・回折モデルは従来どおり共通設定一本化（経路別フィールドは持たせない）。

---

## [2.2] — 2026-06-20

マップウィンドウ（地図クリックによる座標入力／DEM キャッシュ可視化）・HTML レポートへの経路地図添付・起動高速化が主な内容。

### 追加
- **マップウィンドウ**（`views/map_window.py`、旧 `views/tile_manager.py`）: ランチャーのユーティリティボタン「マップウィンドウ」から起動。上部のモードセレクタ（セグメントボタン）でモードを切り替える。
  - **キャッシュ管理モード**: 従来のタイルキャッシュ管理機能（自動カバレッジ表示・ジェスチャ DL/削除）。
  - **座標入力モード**: 地図を素クリックすると TX→RX を交互に指定し、ランチャーの座標欄（start/end）へ自動で書き戻す（数値欄が常に source of truth）。ウィンドウを開いた時点で既存座標を読み込み、マーカー表示＋センタリングする。
    - **端点マーカー**: 半透明シアンのハロー＋ノード（TX=塗り / RX=白抜き）。
    - **経路ライン＋水平距離ラベル**: TX/RX を結ぶ細線（シアン）と、中点に半透明背景つきの距離ラベルを重ねて表示（パン/ズーム追従）。
  - **初期ズームの自動調整**: 開いたとき、設定中の TX/RX の経路長に合わせてズームを自動設定する。
- **シミュレーションレポートへの経路地図添付**（`report_map.py` / `map_graphics.py`）: 個別シミュレーションの `report.html` に、TX/RX・経路・距離を国土地理院 淡色地図へ重ねた静的地図を埋め込む。
  - 経路が常に水平（TX=左 / RX=右）になるよう自動回転し、方角の手がかりに北矢印を重ねる。
  - 出力サイズは地形断面図と同じ縦横比に揃え、レポート上で両者の高さが一致する。
  - タイルは経路にフィットする範囲のみ取得（外部サーバー配慮）。取得できないときは地図を省いて注記を残す（レポート自体は必ず生成）。
- **「アプリ設定を読込む」メニュー**: 設定ファイルから app 設定（テーマ/言語/プロキシ）のみ取り込む。シミュレーション条件は変えない。
- **グラフの縦倍率注記**: 長距離プロファイルで地形が湾曲して見える誤解を避けるため、曲率注記に「縦倍率 約×N」を併記する。
- `models.horizontal_distance_km()`: 2地点間の水平距離（haversine）を返す純関数。距離ラベルと地形プロファイル計算で共有。

### 変更
- **マップは各モードの関心レイヤだけを表示**: キャッシュ管理モード＝カバレッジ塗りのみ／座標入力モード＝経路レイヤ（マーカー/線/距離ラベル）のみ。モードを往復しても座標は保持される。既定モードは「座標入力」（モードセレクタの並びも座標入力を左＝主機能にした）。
- **設定の app/sim 分離**: `save_app`/`save_sim`・`select_app`/`select_sim` でキー群を分離（ファイル形式は不変）。「パラメータ読込」は sim 限定、「アプリ設定読込」は app 限定で、互いの設定を上書きしない。
- **環境区分（環境タイプ）の表記を言語連動で統一**: 地形ウィンドウとランチャー／バッチのドロップダウンが英語（Urban / LoS）、HTML レポートだけ日本語という不整合を解消し、すべて表示言語に追従するようにした（内部値は不変のため既存設定・CSV と互換）。日本語表記は 市街地／郊外／農村／見通し に統一（#11）（幾何の「見通し線（LoS）」は線そのものの略号として維持）。
- **タイルキャッシュの範囲削除は標高（DEM）タイルのみを対象に**: レポート用の淡色地図タイルは範囲削除では消さず、「全キャッシュ削除」でのみ消去する。全削除の確認ダイアログに、標高タイルとレポート地図タイルの両方が消える旨を明記した。
- **起動の高速化**: 同梱ファイル数を削減（未使用の Tcl データ等を除外、1273→約495ファイル）し、未署名 exe に対するウイルススキャンの起動時負荷を軽減。matplotlib を遅延 import 化。
- タイルキャッシュ管理の入口を設定メニューからマップウィンドウへ移行（全キャッシュ削除は設定メニューに残置）。
- 確認ダイアログを親ウィンドウ中央に表示する共通ヘルパー `views/dialogs.py` に集約（バッチウィンドウのダイアログも同ヘルパーへ統一）。
- レポート地図の地名・等高線ラベルを 2 倍スーパーサンプルで鮮明化（追加のタイル取得なし）。
- **README の精度レイヤ説明を訂正**（#10）: 緑のカバレッジ層は「航空写真由来」ではなく航空レーザ測量由来（dem5a）。全 README の表記を修正。

### 修正
- **地図を開いたままウィンドウ／アプリを閉じると Tcl エラーが出る**: `invalid command name "...update_canvas_tile_images"` が出る不具合を修正。地図の after ループを止めてから破棄する手順を 1 つの関数（`close_map_safely`）に集約し、マップウィンドウの×・アプリ終了の双方が同手順を通るようにした（破棄経路ごとに手順をコピーすることによる再発を防止）。あわせて、地形ウィンドウを開いたままランチャーを閉じるとプロセスが終了しない不具合も修正。
- **キャッシュ総量表示の同期**（#9）: マップウィンドウのキャッシュ管理モードを開いたまま個別シミュレーションを実行すると、プリフェッチで増えたタイルがキャッシュ総量・カバレッジ表示に反映されない不具合を修正。
- **HTML レポートの地形グラフが英語のまま**（#12）: 日本語モードでレポートの地形断面 PNG の軸ラベル・凡例（距離／高度／地形／植生／見通し線／第1フレネルゾーン）が英語表記だったのを表示言語に追従するよう修正。
- 座標入力モードで地図左上のズーム +/- ボタンを押すと、その位置が座標として誤登録される不具合を修正。

### ドキュメント
- **リリース前にソース全体とドキュメント群の記載内容を突き合わせて整合を確認**（バージョン文字列だけでなく内容を監査）。以下の実装との乖離を修正:
  - 依存ライブラリに `tkintermapview`（マップウィンドウ用）が未記載だったのを README 群の `pip install` 行・依存表へ追記（`requirements.txt` とも一致）。
  - 開発者向け README のテスト件数が古かった（226 件）のを実数 296 件へ更新し、欠落していた `test_report_map.py` / `test_map_window.py` の行とファイル構成の記載を追加。
  - アーキテクチャのレイヤー構成図に 2.2 の新モジュール（`views/map_window.py` / `views/dialogs.py` / `report_map.py` / `map_graphics.py`）が未反映だったのを、各層（表示／オーケストレーター／純粋描画）へ追記。
  - **再発防止**: ドキュメントのドリフトを自動検出する `tests/test_docs_consistency.py` を追加。コードから生成した正準リスト（モジュール／テストファイル／依存）に対し、README の各構造セクション（ファイル構成ツリー・アーキテクチャ層構成図・テスト表・`pip install` 行）を**個別に**照合する。今回見落とした「図にだけ無い」型のセクション固有ドリフトを CI で捕捉できる。

---

## [2.1] — 2026-06-12

DEM タイルキャッシュを地図上で可視化・管理する機能を追加。

### 追加
- **タイルキャッシュ管理ウィンドウ**（`views/tile_manager.py`）: 設定メニュー →「タイルキャッシュ管理」から起動。GSI 淡色地図上で DEM キャッシュを操作する。
  - **自動カバレッジ表示**: 地図のパン/ズームに追従し、キャッシュ済み領域を半透明塗り＋外周線で常時表示。クアッドツリーで集約（埋まったブロックは粗く、エッジは細かく zoom-14 まで）。最高精度レベルで色分け（5a=緑 / 5b=黄 / dem=水色）。
  - **全ジェスチャ操作**: Ctrl＋ドラッグ=ダウンロード / Ctrl+Alt＋ドラッグ=強制再取得 / Shift+Ctrl＋ドラッグ=範囲削除 / 素のドラッグ=地図パン。DL・削除は確認ダイアログ（新規エリア数・容量目安／実キャッシュ数を提示）。
  - **下部ステータスバー**: 動的メッセージ（アイドル時は操作ヒント、操作中・直後は状態/結果を表示し一定時間後にヒントへ復帰）＋キャッシュ総量＋細線プログレス（アイドル時は高さ予約のみで非表示）。出典表記は地図右下にオーバーレイ。
  - **全キャッシュ削除**: 設定メニュー →「全キャッシュ削除」（中央配置の確認ダイアログ）。
- `infrastructure.py` に公開 API を追加: `prefetch_tiles` / `scan_cache_overlay` / `coverage_outline` / `count_bbox_tiles` / `count_cached_areas` / `delete_tile_cache` / `delete_all_tile_cache` / `get_cache_stats` / `tile_to_latlng` ほか。

### 変更
- **DEM 欠損(128,0,0)とプリフェッチの整合**: 実行時の `get_elevation` がピクセル単位で欠損を下位レイヤーへフォールバックするのに対し、旧プリフェッチはタイル単位で打ち切っていた不整合を解消。`_process_position` を欠損ピクセル認識型の精密降下に変更し、欠損が残る限り下位レイヤー（5a→5b→dem_png）まで取得。dem_png（最下層）の存在を終端マーカーとする。
- **`build.bat` を非対話化**: 末尾の `explorer dist\RadioSimPro` と `pause` を削除（自動/バックグラウンドビルドのハング・ウィンドウポップを防止）。

### 修正
- 長距離地形プロファイルにおける等価地球曲率の扱いを明確化（注記）。

---

## [2.0] — 2026-06-09

マルチウィンドウ GUI アプリとしての基礎一式。一括シミュレーション・多言語・テーマ切替・PyInstaller 配布・DEM マルチレイヤ取得を揃えた。プロキシ環境での SSL 動作（`truststore` ＋ Windows 証明書ストア）と配布 exe の動作を実機で確認済み。

### 追加
- **一括シミュレーション**: `batch.py` ＋ `views/batch_builder.py`。
  - `PathRow` / `PathResult` データクラス、`parse_csv()` / `export_csv()` による CSV 入出力。
  - `validate_rows()` でバッチ全体のバリデーション（ID 重複・座標範囲・同一点チェック）。
  - `run_batch()` でバッチをバックグラウンドスレッドから順次実行。
  - per-path の `report.html`（グラフ Base64 埋め込み）／`path.kml`（TX/RX placemark・地形・LoS・第1フレネルゾーン・遮蔽区間）と、サマリ（`summary.csv` / `summary.html` / `summary.kml`）を出力。
  - **Batch Builder ウィンドウ**: GUI テーブル直接入力と CSV インポートの 2 入力方式。行ドラッグ & ドロップ並び替え。`queue.Queue + root.after` ポーリングでスレッドセーフな UI 更新。ランチャーに BATCH MODE ボタンを追加。
- **多言語対応**: `i18n.py`（en/ja）。メニューで切替・再起動で反映。
- **テーマ切替**: sv-ttk ＋ darkdetect を導入し、`main.py` の `_ThemeManager` が system / light / dark の 3 モードを管理。OS の明暗変化を自動検知してリアルタイム追従する。`views/launcher.py` のメニューバーに Theme → System / Light / Dark を追加し、`DEFAULT_CONFIG` に `"theme": "system"` を追加。
- **個別シミュレーション HTML/KML 出力**: `report.html` / `path.kml` を保存パッケージに追加。
- **PyInstaller ビルド**: `build.bat` / `radiosim.spec`（onedir モード）/ `logo.png`。未使用 matplotlib バックエンド 19 個・不要 stdlib モジュール・numpy test 系を除外。`README_binary_*.md` / `logo.png` をバイナリに同梱。
- **DEM マルチレイヤ取得**: `infrastructure.py` を単一 dem_png から 3 レイヤー優先順位へ刷新。`DEM_LAYERS = [("dem5a_png", zoom=15), ("dem5b_png", zoom=15), ("dem_png", zoom=14)]`（5m航空→5m写真→10m の順でフォールバック。dem1a_png は除外）。
  - キャッシュキーを `(xtile, ytile)` → `(layer_id, xtile, ytile)` の 3 要素タプルに変更（dem5a/dem5b の衝突を防止）。
  - `_failed_tiles: set[tuple]` を追加し、取得失敗タイルへの再リクエストを防止。
  - ロック外でネットワーク取得 → ロック内でキャッシュ書き込みの構造に変更（ロック保持中のブロッキング通信を廃止）。
- **DEM タイル事前取得**（`prefetch_tiles`）: シミュレーション実行前に bbox 内の全タイルを並列プリフェッチ。オフライン利用に対応。
- **並列 DEM 取得**: `daemon=True Thread` ＋ `Semaphore(8)` による並列化（`ThreadPoolExecutor` は tkinter と競合するため使用しない）。
- **回折モデル選択**: Deygout（デフォルト）/ ~~Epstein-Peterson / Bullington~~ を選択可能。
  - 🔴 **訂正（2026-08-09）＝Epstein-Peterson と Bullington は実装されたことがありません**。`v2.0` タグのツリーを確認したところ、この 2 語が現れるのは**この CHANGELOG だけ**で、`.py` には 1 か所もありませんでした（後の版で撤去されたのではなく、**2.0 の時点で記録が実装より先を書いていた**）。選べるのは**当時も今も `Single` と `Deygout` の 2 つだけ**です。⚠️ **記録は書き換えず取り消し線で残します**——「一度そう書いた」こと自体が読み手にとっての事実だからです。
- **企業プロキシ環境対応**: `truststore.inject_into_ssl()` を `main.py` 先頭で呼び出し、Windows 証明書ストアの CA を `requests` に反映。`radiosim.spec` の `hiddenimports` に `truststore` 系を追加。
- **ヘルプメニュー**: README を markdown → HTML → ブラウザ で表示（フォールバック付き）。バージョン情報ダイアログ。
- **スライダー + TextBox**: グラフウィンドウにアンテナ高・降雨強度のスライダーと連動する数値直接入力欄を実装。
- **Windows アプリ体裁**: Per-Monitor DPI Aware・AppUserModelID・EXE ファイルプロパティ（`version.py` から自動生成）・全ウィンドウ共通アイコン（`icon.png` → `icon.ico`）。バイナリ版 README に `logo.png` を base64 インライン埋め込み。
- `version.py` を新規追加（`APP_NAME` / `APP_VERSION` / `COPYRIGHT` / `USER_AGENT` の一元管理）。
- ランチャーにロゴ表示（`logo.png`）とフィールドホバーツールチップ（`_Tooltip` / `_FIELD_TIPS`）を追加。プログレスバーに % 表示（"Fetching terrain… 42%"）を追加。
- `models.py`: `fresnel_zone_radii()` を独立関数として公開。`ENV_COEFFS` から `veg_coeff` 列を削除（Veg Loss と Env Loss を独立加算する設計に統一）。

### 変更
- 主要ウィジェットを `tk.*` から `ttk.*` に置き換え（sv-ttk テーマ適用のため）。
- バッチ CSV のパス別設定（`env_type` / `rain_rate` / `diff_method`）を廃止し、Common Settings に一本化。`PathRow` フィールドと `export_csv()` ヘッダーを整理。
- **k_factor 命名の曖昧さを解消**: 同一ファイル内で「等価地球半径係数」と「ライスKファクター」が同じ名前 `k_factor` を共有していた問題を修正。`TerrainProfile.k_factor` および `calculate_terrain_profile` の引数を `earth_k`（等価地球半径係数・4/3 固定）に、`calculate_propagation` の引数を `initial_k`（入力ライスK）に改名。`PropagationResult` / `LinkBudgetResult` の `current_k` フィールドコメントを「推定ライスKファクター（表示専用、計算不使用）」に更新。
- **i18n ラベルのライスK表記を統一**: バッチ入力欄 `lbl_b_k_factor` を "K係数" / "K-Factor" から "Kファクター（ライス）" / "Rician K-Factor" に変更。グラフパネル・HTML レポートの `pl_k_factor` / `html_k_factor` を "Kファクター" / "K-Factor" から "ライスK（推定）" / "K-Factor (est.)" に変更。ランチャーの k_factor ラベルも「初期Kファクター（ライス）」とし、ツールチップを Rician K-factor の説明に変更。
- `load_config()` を `dict.update()` から `DEFAULT_CONFIG` キー選択的上書き方式に変更（未知キーを無視、欠損キーはデフォルト維持・不正キー混入防止）。
- `infrastructure.py`: `User-Agent` を `version.USER_AGENT` に統一し、`_get_session()` のセッション生成時に `headers.update()` で設定する（リクエスト単位ではなくセッションレベル。将来のエンドポイント追加でもヘッダー漏れが起きない）。`_VALID_ENV_TYPES` / `_VALID_DIFF_METHODS` を frozenset で定義し `validate_config()` で参照（`batch.py` からも利用可能に）。
- `infrastructure.py` に `_enumerate_bbox` / `_download_tile_set` / `count_bbox_tiles` を追加（タイル管理メニュー向けに流用可能な API）。
- ランチャーのボタンレイアウトを grid で横並びに整理（個別/一括: `Accent.TButton`、ユーティリティ: 下段）。
- `bind_all("<MouseWheel>")` を `batch_builder` の Toplevel `<Destroy>` で `unbind_all` するよう修正（他ウィンドウへのスクロール漏れを防止）。
- **`_NU_THRESHOLD` のコメントを修正**: 「見通しとみなす」という曖昧な表現を「回折損 0 dB として打ち切る（ITU-R P.526 の見通し判定相当）」に変更し、コードの動作を直接説明するようにした。

### 修正
- **Kファクターの概念混在を解消**: ランチャーの `k_factor` はライスKファクター（見通し/散乱電力比・表示専用）であり、リンクバジェット計算には影響しない。等価地球半径係数は内部で 4/3（標準大気）固定に変更し、`graph.py` / `batch.py` の `calculate_terrain_profile` 呼び出しから `k_factor=params.k_factor` を削除。あわせて `DEFAULT_CONFIG["k_factor"]` の初期値を、誤って設定されていた `"1.333"`（等価地球半径係数の値）から `"10.0"`（ライスKとして適切な値）に修正。
- **プロキシ設定後にキャンセルすると次回起動時に設定が消える**: `_on_run` で `save_config(c)` に渡す dict に `proxy_url` が含まれておらず、シミュレーション実行のたびに config ファイルから `proxy_url` が除去されていた。`proxy_url` を `self.config` からコピーするよう修正。
- **プロキシ未設定で実行後にプロキシを設定しても地形が 0m のまま**: `set_proxy()` でセッションをリセットしても `_failed_tiles`（タイル取得失敗セット）がクリアされないため、失敗したタイルが再取得されなかった。また `_terrain_cache`（地形プロファイルキャッシュ）に 0m で保存された結果が残り続けた。`set_proxy()` 内で `_failed_tiles.clear()` を追加し、プロキシダイアログの OK 時に `sim.clear_terrain_cache()` を呼ぶよう修正。
- **連続シミュレーション時のプログレスバー表示崩れ**: `_download_tile_set` の `_worker` で `work_q.task_done()` を `progress_cb` より先に呼んでいたため、`work_q.join()` が最後のコールバック発火前に返る競合があった。`task_done()` を `progress_cb` の後に移動し、Phase 1 の全 `root.after()` が確実にキューに積まれてから Phase 2 が開始されるよう修正。
- `_vegetation_loss` の第一引数に `veg_top`（`elevs + veg_h`）を正しく渡すよう修正（`elevs` 直渡しバグ）。
- `_env_loss` に `veg_loss` を渡さないよう修正（Veg Loss と Env Loss の二重計上を解消）。
- **`radiosim.spec` の truststore hiddenimports 更新**: truststore がモジュール名を変更（`_api_windows` → `_api`、`_stdlib_ssl` → `_windows`）。旧名のまま残っていたためビルドログに ERROR が出ていた。現行名に修正し `_ssl_constants` を追加。
- **バッチ Common Settings ラベルの残留記述を削除**: `batch_common_cfg` に「経路別設定が優先」と記載されていたが、当該機能（`PathRow` の env_type / rain_rate / diff_method）は廃止済みのため除去。

---

## [1.5] — 2026-05

伝搬モデルの拡充（Deygout 多重回折・環境区分・降雨/大気減衰）と、DEM 取得の並列化・キャッシュ。

### 追加
- **Deygout 多重回折**: `_deygout_loss()` 再帰関数を追加。主障害物を再帰的に分割し損失を加算。再帰打ち切り条件: ν < -0.8 または区間幅 < 50 m、深さ上限 20。
  - `_nu()` ヘルパー関数（Fresnel-Kirchhoff パラメータ ν の計算）を追加。
  - `_diffraction_loss_fk(v)` として FK 損失式を独立関数に分離。
  - `calculate_propagation()` に `diff_method` 引数（"single" | "deygout"）を追加。デフォルト: "deygout"。
- **環境区分 (`env_type`)**: `ENV_COEFFS` テーブル（urban / suburban / rural / los 等 7 パラメータ係数）を追加し、`_env_loss()` を環境区分ごとの係数テーブル参照方式に刷新（旧は固定係数）。`DEFAULT_CONFIG` に `"env_type": "los"` を追加、`validate_config()` に `env_type` バリデーションを追加、ランチャーに Env Type コンボボックスを追加（LOAD SETTINGS で復元）。
- **降雨減衰**: `calculate_rain_loss(freq_mhz, rain_rate_mmh, slant_dist_km)` を追加（ITU-R P.838-3 準拠）。
- **大気減衰**: `calculate_gas_loss(freq_mhz, slant_dist_km)` を追加（ITU-R P.676-13 Annex 2 準拠）。
  - `PropagationResult` / `LinkBudgetResult` に `rain_loss` / `gas_loss` フィールドを追加。
  - `calculate_propagation()` / `run_calculation()` に `rain_rate` 引数を追加。
  - 保存 report.txt に `Rain Loss` / `Gas Loss` / `Diff Model` / `Env Type` を追加。
  - グラフウィンドウに Rain Rate スライダー（0〜100 mm/h）と、回折モデル切り替えボタン（Deygout / Single）を追加。
- **並列 DEM 取得**: `simulation.py` に `Semaphore(8)` + `daemon=True Thread` による並列フェッチを実装。完了順でなく座標順に整列して返す。`threading.Event` で全スレッド完了を同期。
- **地形キャッシュ**: `fetch_elevations_cached()` を追加。キャッシュキー `(lat_tx, lon_tx, lat_rx, lon_rx, num)` でヒット時は DEM 再取得なし。`_terrain_cache_lock` で保護。
- `README_ja.md` / `README_en.md` を追加（最初の開発者向け README）。

### 変更
- `infrastructure.py` の `DEFAULT_CONFIG` に `"rain_rate": "0.0"` / `"diff_method": "deygout"` を追加し、`VALIDATION_RULES` に `rain_rate` の範囲チェック（0〜200 mm/h）を追加。
- `views/launcher.py`: `fetch_elevations_cached()` を呼び出すよう変更。`rain_rate` / `diff_method` を `c.setdefault()` で config から補完する処理を追加。

---

## [1.0] — 2026-05

### 追加
- **マルチモジュール分離**: 単一スクリプトから以下のモジュール構成に完全移行。
  - `infrastructure.py`（通信・キャッシュ・設定・バリデーション）
  - `models.py`（副作用ゼロの純粋計算。numpy/math のみ）
  - `simulation.py`（DEM 取得・計算・保存の統括）
  - `views/launcher.py` / `views/graph.py`（UI）
  - `main.py`（エントリーポイント）
- `models.py` にデータクラスを導入（`TerrainProfile` / `PropagationResult` / `LinkBudgetResult`）。
- TX/RX アンテナゲインを個別化（`gain_tx` / `gain_rx`）。旧来は `gain * 2` で共有。
- EIRP = P_tx + G_tx、P_rx = EIRP + G_rx - total_loss の式に統一。
- `actual_margin = P_rx - Sensitivity` を計算・表示。
- `_env_loss()` 関数（経験的環境損失: 3〜30 dB）を追加し合計損失に組み込み。
- 植生減衰の周波数帯域別係数（< 1 GHz / 1〜6 GHz / > 6 GHz）を導入。
- `VALIDATION_RULES` 辞書による入力検証、TX/RX 同一点チェックを追加。
- `setup_logging()` で ファイル＋コンソール二重出力のログを統合。
- DEM タイル取得を `_fetch_tile()` に分離し、無効値ピクセル `(128, 0, 0)` を 0.0 に変換。
- 保存レポートに `EIRP` / `Act Margin` / `Env Loss` を追加。
- テストディレクトリ（`tests/`）を追加。

### 変更
- Fresnel 第1ゾーン半径の分母を `d1 + d2` に修正（旧: `horiz_dist_km * 1000`）。

---

## [プロトタイプ期] — 2026-05-10 〜 2026-05-12

単一スクリプト構成で段階的に機能を追加した開発初期。ファイル名にバージョンが埋め込まれていた。

- `radio_sim.py`: 最初期。requests + PIL で国土地理院 DEM PNG を取得し地形プロファイルを描画。自由空間損失・回折損なし。
- `radio_sim_gui.py`: tkinter 入力フォームを追加。地球曲率補正（Ke=4/3）・Fresnel 第1ゾーン描画・回折損（簡易 FK 近似）・FSPL 計算・P_rx 表示を初実装。
- `antenna_slider_sim_v5〜v8`: matplotlib スライダー（TX/RX アンテナ高）によるリアルタイム更新、`config.json` でのパラメータ永続化、tkinter ランチャー + matplotlib グラフの 2 ウィンドウ構成、`blocked_ratio > 100%` 時の回折補正を実装。
- `radio_sim_ultimate_Rel1.0〜Rel1.3`: ディスクキャッシュ・植生レイヤー・K ファクター・受信感度・SAVE PACKAGE・別スレッドでの DEM 取得（`threading.Thread`）・進捗バーを追加。数値安定化（`np.nan_to_num`）・デバウンス（50ms）・キャッシュ排他制御（`threading.Lock`）を段階的に改善。著作権表示 "© 2026 BearValley AI Craftworks" を追加。
